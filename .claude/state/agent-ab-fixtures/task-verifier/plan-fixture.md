# Plan: Slugify refactor (FIXTURE)
Status: ACTIVE-FIXTURE (synthetic — not a real plan; do not archive)

## Goal
Refactor `slugify.sh` into `slugify-v2.sh` (cleaner pipeline, same behavior).
Done when: `slugify-v2.sh` passes the ORIGINAL pre-existing test suite
(`original-tests.sh`) without modification.

## Tasks
- [ ] T-TV-1. Produce slugify-v2.sh behaviorally identical to slugify.sh —
  Verification: full
