## Summary
§E.W integration cutover (Wave-E wiring) — brings the merged E.1–E.12 hooks live in the template + manifest. Built on a branch and validated; the controlled live install + verification battery + E.7 drill runs per the E.W cutover premortem during a coordinated freeze window (Session A is coordinating; this PR is the merge vehicle).

- **Template wiring** (fd61307): Stop chain 6→4 — the 3 blocking gates (work-integrity/session-honesty/bug-persistence) replaced by `stop-verdict-dispatcher.sh` (E.11, invokes them in `--report` mode and aggregates one verdict); SessionStart entry 8 `surfacer-pack`→`session-start-digest` (E.1); `context-watermark` added to PostToolUse (E.9a); `pre-compact-continuity` added to a new PreCompact block, auto+manual (E.9b). JSON-validated; Stop=4, SessionStart=8, PreCompact=2.
- **E.1↔E.8 field-schema fix** (7204d19): the digest's `feed_nl_issues` now calls `nl-issue.sh --digest-feed` — the old inline reader grepped `"triaged":false` but nl-issue.sh writes `"triage_status":"untriaged"`, so it always reported 0. Clears the doctor `wave-e-e8-nl-issue` RED.
- **Manifest merge** (943f009): 11 Wave-E fragment entries merged into manifest.json; `PreCompact` added to the schema events enum (pre-compact-continuity migrated off the `manual` stand-in); `scripts/`-prefixed hooks[] normalized (schema no-slash pattern); the 4 entries the template rewrite un-wired (3 gates now via dispatcher, surfacer-pack→digest) reconciled to `wired_template:false`. `manifest-check.sh` GREEN (90 entries, 100 hooks).
- **Governance items** (95695f3): dispatcher `pin-f-doctor-exempt` marker (it aggregates member-gate verdicts; the members validate purpose clauses); `.gitignore` += `NEEDS-YOU.md` (E.6); nl-issue SKILL.md now prohibits direct NL-repo/`~/.claude` writes from foreign-project sessions (operator-endorsed; downstream-product-incident golden scenario).

## What mechanism would have caught this?

### a) Existing catalog entry

This is the wiring step for already-merged, already-verified hooks; the enforcement here is the harness-doctor itself — before this cutover the live doctor showed exactly the 8 reds this PR resolves (6 lib-deps for the un-installed purpose-clause lib, manifest-check, wave-e-e8-nl-issue), i.e. the doctor mechanically produced this PR's checklist. Post-install `doctor --quick` GREEN is the acceptance oracle, per the premortem's verification battery.

## Verification
- Branch: `manifest-check.sh` GREEN (90 entries, 100 hooks, 0 warn); template JSON valid, chains Stop=4/SessionStart=8/PreCompact=2 asserted by node.
- The live install + `doctor --quick`+`--full` GREEN + golden 6/6 + synthetic 8/8 + E.7 kill-and-resume drill + heartbeat schtasks are the premortem's post-merge battery (run during the coordinated freeze window before declaring done) — not this PR's CI, which validates the branch's static correctness.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
