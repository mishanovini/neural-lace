## Summary
- Root-caused the reported `decision-context-gate.sh --self-test` 6-scenario failure (ST2/ST5×2/ST11/ST12/ST28): **not a logic regression** — it is the tracked HARNESS-GAP-42 cold-environment class (zod unresolvable because linked worktrees don't share the main checkout's gitignored `node_modules`; the validator degrades to allow+fallback by design). The gate has been in `hooks-selftest.yml` `KNOWN_FAILING_HOOKS` for exactly this class since 2026-06-02.
- Found and fixed a **new class defect introduced by the D.5 cutover** (PR #73): 16 retired gates moved to `attic/` but not the `hooks/lib/` they source relative to their own path — 13 source `lib/stop-hook-retry-guard.sh` unguarded and die on execution.
- Fix: three one-line re-source shims at `attic/lib/` pointing at `../../hooks/lib/`, restoring runnability of every archived gate without duplicating library code or modifying archived bytes.
- Recorded as NL-FINDING-018 in `docs/findings.md`.

## Attribution matrix (attic/decision-context-gate.sh --self-test, run in a linked worktree)
| Configuration | Result |
|---|---|
| Unfixed | 19 pass / 7 fail (lib sourcing errors pollute stderr) |
| With attic/lib shims, no zod | 20 pass / 6 fail — exactly the reported failure set |
| With shims + `DECISION_CONTEXT_SCHEMA`/`CONV_TREE_STATE_LIB` at a zod-equipped checkout | **29 pass / 0 fail** |

## What mechanism would have caught this?

### b) New catalog entry proposed

NL-FINDING-018 (docs/findings.md, in this PR) names the class: a relocation that moves scripts but not the directories they `source` relative to `${BASH_SOURCE[0]}`. Nothing executes attic/ today, so no existing gate could fire. A cheap detector for the class: extend the hooks-selftest discovery sweep (or a shellcheck-style source-resolution smoke) to `attic/*.sh` so a broken relative `source` surfaces on the PR that breaks it, with allowlist semantics matching the existing KNOWN_FAILING_HOOKS mechanism.

## Primary evidence (required for any sweep / class-fix / refactor PR)

### What runtime/log evidence did you pull?

Ran `bash adapters/claude-code/attic/decision-context-gate.sh --self-test` in the linked worktree and captured full output; separately ran `node -e "require('zod')"` from `neural-lace/workstreams-ui/state/` and checked `node_modules` presence in both the worktree and the main checkout.

### What did the evidence show?

Verbatim: `line 90: .../attic/lib/stop-hook-retry-guard.sh: No such file or directory`, then `line 636: retry_guard_session_id: command not found`, summary `self-test: 19 pass, 7 fail`. And `Cannot find module 'zod'` from the worktree (no `node_modules`), while the main checkout has zod installed per `workstreams-ui/package.json`. Grep showed 13 attic gates share the unguarded `source "${BASH_SOURCE[0]%/*}/lib/stop-hook-retry-guard.sh"` line.

### What hypothesis did you test BEFORE writing the fix?

HYPOTHESIZED two independent causes — (1) D.5 moved gates without their lib/ directory, and (2) the remaining failures are HARNESS-GAP-42 cold-env (zod), not a logic regression. Tested by adding the attic/lib shims (failures dropped 7→6, exactly the originally-reported set) and then pointing `DECISION_CONTEXT_SCHEMA`/`CONV_TREE_STATE_LIB` at the zod-equipped main checkout (29/29 pass).

### What refutation criteria would have shown the hypothesis was wrong?

REFUTED if any scenario still failed with shims in place and a zod-resolvable schema module — that residue would have been a genuine logic regression in the fence-validation path requiring a code fix rather than an environment attribution. Observed: zero residual failures (29/29).

## Notes
- CI no longer discovers this self-test (live path is an exit-0 shim without the `--self-test` token; attic/ is not swept), so the `KNOWN_FAILING_HOOKS` entries for `decision-context-gate.sh` and `decision-context-replay.sh` are now inert — noted in the finding for a future workflow touch-up, deliberately not bundled here.
- The GAP-42 real fix (self-contained self-tests / dependency install) remains the separately-owed follow-up; out of scope per the task's own moot-check guidance since D.5 retired the gate.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
