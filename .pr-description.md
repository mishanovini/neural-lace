## Summary
Wave-E batch 1: five file-disjoint builder branches merged (build-parallel, verify-sequential per orchestrator-pattern; every branch tip verified on disk against the builder's report and every self-test re-run independently by the orchestrator before merge).

- **E.1** `hooks/session-start-digest.sh` (20/20 self-test) — ONE ≤15-line SessionStart block replacing the surfacer-pack's voice (wiring lands at the §E.W integration step, not here); dedup/auto-expiry/auto-ack state; feeds tolerate not-yet-built E.3/E.8/E.11 surfaces. NL-FINDING-021 fixed at the emission site (no alert on empty-anomaly/healthy) and the **33** stale principles-gate-r3 alerts acked live as one class with one ledger event (live unacked count now 0, verified).
- **E.2** work-integrity-gate self-test per-scenario isolation (NL-FINDING-025 refutation: 5 consecutive 24/24 runs recorded in the finding) + `scripts/purge-selftest-pollution.sh` (dry-run default, self-tested).
- **E.7** `scripts/session-resumer.sh` (10/10) — death-signature detection (429/529/stale-with-in-flight), marker-aware skip, backoff 5→15→45→120 max 5, fresh-spawn fallback, ledger + digest-feed events. Registration + live kill-and-resume drill are §E.W orchestrator steps by spec.
- **E.8** `scripts/nl-issue.sh` (24/24) + `skills/nl-issue/SKILL.md` — cross-project capture (proven from a foreign cwd), triage verbs, digest/KPI consumption contracts documented for E.1/E.5.
- **E.9** `scripts/session-snapshot.sh` + `hooks/context-watermark.sh` + `hooks/pre-compact-continuity.sh` (13+11+10) — two-stage pre-compaction continuity; all SIX normative preserve-list categories grep-proven in the emitted summarizer instructions; PreCompact injection channel honestly HYPOTHESIZED with the PROVEN snapshot-file + SessionStart compact-echo fallback implemented (constitution §1 pin).
- Serialization rules held: zero edits to settings.json.template / manifest.json / harness-doctor.sh / install.sh on any branch (grep-verified per branch); manifest fragments + doctor predicates shipped under `tests/fixtures/wave-e/E.*/` for the §E.W merge and the E.10 doctor builder.

## What mechanism would have caught this?

### a) Existing catalog entry

This PR is builds-to-spec, not a defect fix; the enforcement relevant here is the wave's own serialization discipline (specs-e §E.0.1), which the orchestrator verified mechanically per branch (forbidden-surface diff = empty). The two defects fixed in passing were both already ledger-tracked as findings with assigned tasks: NL-FINDING-021 (E.1) and NL-FINDING-025 (E.2) — the findings ledger is the mechanism, and both entries now carry their remediation/refutation records.

## Verification
- Orchestrator re-ran every suite on the INTEGRATED tree (not builder worktrees): digest 20/20, work-integrity 24/24, resumer 10/10, nl-issue 24/24, snapshot 13/13, watermark 11/11, pre-compact 10/10, purge-script PASS.
- Golden evals 6/6 PASS on the integrated tree.
- E.2's temp-HOME hash proof (review finding 9) runs as the orchestrator's background job against this tree; its verdict gates the E.2 checkbox flip (task-verifier), not this merge — the merge oracle is the suites above.
- Live-machine side effects verified: external-monitor unacked alert count = 0; exactly one class-ack ledger event.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
