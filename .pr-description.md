## Why

Master is RED: the golden test `evals/golden/rules-index-coverage.sh` (invariant 4 — the 3000-byte hard cap on every non-`-full` doctrine compact) fails because two compacts exceeded it, so the **Golden behavioral tests** check blocks on every PR to master:

- `adapters/claude-code/doctrine/git.md` — 3653 bytes
- `adapters/claude-code/doctrine/orchestrator-pattern.md` — 3451 bytes

## What

Applied the harness's own prescribed remedy (the fix message the golden test itself prints): trim each compact to its load-bearing rules + the `Full:` pointer, and relocate overflow **detail** to the paired `<name>-full.md` companion (precedent: `frontend-conventions-full.md`, `vaporware-prevention-full.md`). **No doctrine content was lost — only relocated.**

| File | Before | After | Headroom |
|---|---|---|---|
| `doctrine/git.md` | 3653 | **2974** | 26 B |
| `doctrine/orchestrator-pattern.md` | 3451 | **2965** | 35 B |

- `git.md`: the deploy-preflight and plan-rooted-`plan:`-trailer detail were **not** present in `git-full.md`, so they were **added** to `git-full.md` (15418 → 17847 B, cap-exempt) as two new sections *before* the compact was trimmed. Every rule (NEVER force-push, no `--no-verify`, post-merge sync, staged-set verify, stop-hook waiver, branch hygiene, PR-merge, deploy-default, deploy-preflight-mandatory, customer-tier, wrong-account) stays in the compact.
- `orchestrator-pattern.md`: the shared-checkout disciplines and proactive-audit-loop detail (6 dead-code patterns, incidents 2026-06-18/07-06/07/09) **already** lived in `orchestrator-pattern-full.md`, so those bullets were condensed in place with no `-full` edit.

## Primary evidence

- `bash evals/golden/rules-index-coverage.sh` → exit 0, `Checks passed: 4`, `PASS`.
- Both compacts ≤ 3000 B (`wc -c`).
- **harness-reviewer: PASS — land it.** Independently traced every relocated passage into the `-full` companions (no rule dropped, no content lost); confirmed the two new `git-full.md` sections were net-new at the parent (no duplication) and the edited text is denylist-clean.

Plan (closed, COMPLETED): `docs/plans/archive/doctrine-compact-bytecap-fix-2026-07.md`; per-task mechanical evidence under `docs/plans/doctrine-compact-bytecap-fix-2026-07-evidence/`.

## What mechanism would have caught this?

The golden test `evals/golden/rules-index-coverage.sh` (invariant 4) DID catch the over-cap compacts — but only at CI/PR time, as a hard block after the drift had already merged onto the compacts. There is no edit-time or near-cap signal, so an author trimming a compact has no warning until CI reds. The compacts also land only 26–35 B under the cap, so the next incidental JIT edit re-trips the same hard block.

### b) New catalog entry proposed

Proposed a soft **near-cap warn threshold** in `harness-doctor.sh`: surface any non-`-full` doctrine compact whose byte count exceeds ~2950 B (within ~50 B of the 3000-byte hard cap), so the near-cap class is flagged locally before the golden test's hard cap trips in CI. Filed to the cross-project ledger via `nl-issue.sh` (surfaced by harness-reviewer during `doctrine-compact-bytecap-fix-2026-07`). This complements the existing hard-cap catch (the golden test) with an earlier, non-blocking signal at the edit/doctor layer.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
