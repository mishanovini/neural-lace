## Summary
- **ADR 059** (operator-approved 2026-07-03): session-end enforcement redesign — batched Stop verdict, block-once-then-ledger, session-scoped invariants, waiver parity, remedy-chain analysis, session end-manifest, metric-driven auto-demotion. Amends ADR 058 D5's chain *semantics*; census budgets unchanged.
- **Program plan**: three new tasks carry the mechanics — E.8 (batched verdict + block-once-then-ledger), E.9 (end-manifest + world-state-assertion sweep), F.5 (governance: waiver parity rule, remedy-chain checklist for harness-reviewer, auto-demotion lifecycle) — plus a Decisions Log entry recording the operator approval.
- **Gate refinement**: reconciles the two check (a) waiver valves built in parallel (master's NL-FINDING-020 valve, 214541e, vs this branch's ADR 059 valve). Kept from master: dedicated `work-integrity-waiver-<slug>-*.txt` filename (one file no longer clears both checks (a) and (b)) + main-checkout lookup for worktree sessions + block message. Added on top (ADR 059 D4): the `is_completed` guard (a waiver cannot clear the COMPLETED-but-unchecked contradiction), `pending=0` instead of `return 0` (a waiver cannot clear the checked-box-without-evidence honesty check), and `_wig_ledger "waiver"` telemetry so E.3's waiver-density alarm counts every use.
- Self-test grows 20→23 scenarios (2b valve-pass, 2c honesty-scoping on a mixed plan, 2d COMPLETED guard); NL-FINDING-019 dispositioned; DECISIONS.md index row added.

## What mechanism would have caught this?

### b) New catalog entry proposed

ADR 059 D5 itself is the proposed mechanism: a mandatory remedy-chain analysis in harness-reviewer's checklist (for each remedy a gate's block message prescribes, which other gates does executing that remedy trigger?), landing via program task F.5. Both NL-FINDING-016 (fix+commit compound → commit-gate re-block) and NL-FINDING-019 (mandated scope-line append → plan-ownership block) are the golden examples the analysis would have caught pre-ship. D4's waiver-parity rule (every blocking check ships a structured waiver path) is the companion mechanism preventing the no-exit deadlock class.

## Primary evidence (required for any sweep / class-fix / refactor PR)

### What runtime/log evidence did you pull?

The live four-cycle Stop trap of 2026-07-03 (session that shipped PR #74): verbatim block messages from work-integrity check (a) across four Stop attempts, the retry-guard counter file (`stop-hook-retries-work-integrity-gate-*.txt` reading count 2 at SHA ef69f49), and the retry-guard downgrade stanza. Then `bash adapters/claude-code/hooks/work-integrity-gate.sh --self-test` runs before and after each change.

### What did the evidence show?

Check (a) blocked with "complete 20 tasks or set Status: ABANDONED" — both wrong for a session whose only plan edit was the scope line scope-enforcement mandates — and offered no waiver path; the session ended only via the retry-guard's emergency valve. Post-reconciliation self-test: 22 passed / 1 failed — scenario 2b failed (expected 0, got 2) because with the dedicated waiver filename the check (a) waiver no longer doubles as check (b)'s acceptance waiver, so check (b) blocked the fixture on the missing acceptance artifact.

### What hypothesis did you test BEFORE writing the fix?

HYPOTHESIZED the 2b failure was fixture isolation (check (b) interaction), not valve logic — the real nl-overhaul plan is acceptance-exempt (line 6) so no live impact. Tested by adding the same `acceptance-exempt: true` header scenario 3/4 fixtures use: self-test went 23/23.

### What refutation criteria would have shown the hypothesis was wrong?

REFUTED if the 2b failure had persisted with the exempt header (would have meant the valve itself mis-fires), or if scenarios 2c/2d had failed (would have meant the honesty scoping regressed the block paths). Observed: 23/23 after the one-line fixture fix; 2c and 2d passed in every run.

## Verification
- `work-integrity-gate.sh --self-test`: **23 passed, 0 failed** (was 20 scenarios pre-change; DONE-refusal regression scenario included and green).
- harness-reviewer review dispatched on the gate change (mandatory for hook edits); merge held until its verdict is addressed.
- CI `hooks-selftest` runs the same suite server-side on this PR.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
