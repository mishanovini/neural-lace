## Summary

Wave-E E.1 discovered that this machine's global `core.hooksPath` (set by `install.sh` Phase 4) points at the real harness `git-hooks/` dispatchers, so **every fixture `git commit` inside a hook self-test fires the REAL pre-commit scanner + post-commit installer-sync, and every fixture push fires the real pre-push chain** — which hung `session-start-digest.sh --self-test` indefinitely (fixed in the E.1 branch). This PR sweeps the class across the estate: **43 fixture `git init` sites in 25 hook files** now set `git config core.hooksPath ""` immediately after `git init` (42/24 in the original sweep + `pre-compact-continuity.sh`, which landed on master mid-review via Wave-E batch 1 [#79] with the same latent class and was caught by re-running the sweep post-merge). No assertion changes — fixture setup robustness only. Ledger entry: NL-FINDING-029 in `docs/findings.md`.

**Scoping:** 6 files with fixture `git init` but no real commit/push were intentionally left untouched (`prd-validity-gate`, `pre-commit-tdd-gate`, `propagation-trigger-router`, `wire-check-gate`, `harness-hygiene-scan`, `spec-freeze-gate` — their `git commit` strings are comments/echo text/simulated `CLAUDE_TOOL_INPUT` payloads). Bare-repo inits skipped (no working tree; no server-side receive hooks in the dispatcher dir). Cross-ref: hypothesized contributor to NL-FINDING-025's parallel-load flakes.

## What mechanism would have caught this?

### b) New catalog entry proposed

Class: **self-test fixture repos inherit machine-global git config, so fixture git operations execute the real harness hook chain** (CI-invisible: runners have no global `core.hooksPath`, so hooks-selftest.yml structurally cannot catch it — it only bites on a fully-installed harness machine, i.e. the operator's). Proposed codification, recorded in NL-FINDING-029: (1) regression sweep query `grep -l "git init" adapters/claude-code/hooks/*.sh adapters/claude-code/hooks/lib/*.sh | xargs grep -iL 'core.hookspath'` (`-i`: the estate has both spellings — E.1's digest fix is lowercase) — any hit whose fixture commits or pushes is a recurrence; candidate home is a harness-hygiene-scan Layer-2 heuristic or a doctor check so the class is flagged at commit time, not discovered by the next hang. The class already RECURRED once during this PR's own lifetime: `pre-compact-continuity.sh` merged to master (#79) with a fresh instance — caught and fixed here by re-running the sweep, which is exactly why the codified check should exist. (2) E.2 generalization: one `export GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=core.hooksPath GIT_CONFIG_VALUE_0=""` at self-test entry (git ≥2.31) sandboxes every repo a suite creates, making the class unexpressible instead of per-fixture-remembered. Wiring either is Wave-E follow-up work, not this sweep.

## Primary evidence (required for any sweep / class-fix / refactor PR)

### What runtime/log evidence did you pull?

- `git config --global --get core.hooksPath` on this machine → the real `adapters/claude-code/git-hooks` dir; `ls` of that dir → `pre-commit`, `post-commit`, `pre-push` dispatchers exist (all three fire machine-wide).
- Isolated single-commit timing in a throwaway fixture: commit with global hooks in effect vs with a local `core.hooksPath ""` override.
- HEAD-baseline run of the unmodified `bug-persistence-gate.sh --self-test` (lib reconstructed from `git show HEAD:`) vs the fixed version.
- Full sweep of all 24 edited suites (sequential, 60s ceiling, per-suite logs) + isolated re-runs of the 7 that exceeded 60s (300s ceiling) + a final 590s run of `workstreams-emit`.

### What did the evidence show?

- Per-commit mechanism: **15.6s with real hooks vs 0.4s with the override (~36×)** — so a suite with 10+ fixture commits pays minutes of pure hook overhead, and anything blocking in the chain (post-commit runs install.sh) becomes the E.1 indefinite hang.
- HEAD baseline: `bug-persistence-gate` timeout-killed at 120s having completed only 4 scenarios; fixed version finishes the full suite green in 61s.
- Verification: **all 24 suites pass** — 17 green in-sweep (8–61s); isolated: stop-hook-retry-guard 62s, plan-auto-closure 65s, plan-deletion-protection 74s, session-start-auto-install 64s, work-integrity 132s, scope-enforcement 153s, workstreams-emit 484s (66/66 scenarios; CI-allowlisted for unrelated cold-CI deps). The 4 mid-sweep 60s kills were partly self-inflicted (concurrent baseline experiments); all pass isolated. The >60s durations are suite size on Windows bash — this change strictly removes per-commit work.

### What hypothesis did you test BEFORE writing the fix?

That a local `core.hooksPath ""` in a fixture repo suppresses the machine-global hook chain and the fixture commit completes fast and clean — tested in a scratch repo BEFORE editing any hook (commit completed rc=0 in 0.096s with the override; effective hooksPath read back as empty). Also verified per-file which `git commit`/`git push` mentions are real executed operations vs simulated `CLAUDE_TOOL_INPUT` JSON strings before deciding the 24-file scope.

### What refutation criteria would have shown the hypothesis was wrong?

- The override test commit still taking ~15s or the real scanner still firing (would refute local-config precedence over `--global`).
- Any of the 24 suites failing post-fix with an assertion error absent pre-fix (would show the override changed behavior under test, not just fixture setup) — none did; all 24 pass.
- The fixed `bug-persistence-gate` not improving on its HEAD baseline (would refute the hook-tax attribution of the slowness).
- For the NL-FINDING-025 cross-ref (kept HYPOTHESIZED, not claimed fixed): the work-integrity flake recurring after this fix refutes the contributor hypothesis.

## Verification — all 24 suites pass

Sequential sweep (60s ceiling): 17 green —
| hook | rc | s | hook | rc | s |
|---|---|---|---|---|---|
| backlog-plan-atomicity | 0 | 19 | review-finding-fix-gate | 0 | 11 |
| bug-persistence-gate | 0 | 61 | session-start-git-freshness | 0 | 38 |
| decisions-index-gate | 0 | 10 | session-start-worktree-advisor | 0 | 8 |
| definition-on-first-use-gate | 0 | 36 | stale-active-plan-surfacer | 0 | 18 |
| docs-freshness-gate | 0 | 11 | pre-push-divergence-check | 0 | 22 |
| automation-mode-gate | 0 | 50 | migration-claude-md-gate | 0 | 12 |
| findings-ledger-schema-gate | 0 | 38 | plan-lifecycle | 0 | 45 |
| doc-gate | 0 | 50 | lib/nl-paths | 0 | 10 |
| migration-naming-gate | 0 | 41 | | | |

Heavyweights, re-run in isolation:
| hook | rc | s |
|---|---|---|
| lib/stop-hook-retry-guard | 0 | 62 |
| plan-auto-closure | 0 | 65 |
| plan-deletion-protection | 0 | 74 |
| session-start-auto-install | 0 | 64 |
| work-integrity-gate | 0 | 132 |
| scope-enforcement-gate | 0 | 153 |
| workstreams-emit | 0 | 484 (66/66 scenarios) |

Post-merge of Wave-E batch 1 (#79) into this branch:
| hook | rc | s |
|---|---|---|
| pre-compact-continuity (new on master; class instance fixed here) | 0 | 22 (10/10) |
| work-integrity-gate (re-run: merged master's +33 lines with this branch's fixture fix) | 0 | 162 (24/24) |

Estate sweep re-run post-merge is clean: the only hooks with fixture `git init` and no override are the six intentionally-untouched no-real-git-ops files.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

