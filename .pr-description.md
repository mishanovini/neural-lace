## Summary
Wave-E batch 3 (final builder batch): ADR 059's session-end enforcement mechanics. One builder (E.11 + E.12 rewrite the same Stop-gate cluster). Branch tip verified on disk; all self-tests re-run independently by the orchestrator; golden + synthetic green on the integrated tree.

- **E.11 — batched Stop verdict + block-once-then-ledger** (ADR 059 D1/D2): new `hooks/stop-verdict-dispatcher.sh` invokes the three blocking Stop gates in a new `--report` mode (each runs all checks, exits 0, emits its gap list as JSON — normal blocking + `--self-test` modes preserved unchanged), aggregates into ONE combined block on the first Stop, and on a second Stop with unresolved gaps writes them to the unresolved-gaps ledger + NEEDS-YOU.md + digest and ends (protocol-downgrade, ledger-logged). The DONE-refusal is retained verbatim — a verification-class gap under a DONE: claim is never downgraded (regression-tested).
- **E.12 — session end-manifest** (ADR 059 D6/D3): `schemas/end-manifest.schema.json` + `scripts/end-manifest.sh` (write/validate). The validator checks claims mechanically (SHA reachable from the remote, unresolved item recorded where claimed, worktree clean, marker matches transcript last line). Manifest scoping replaces work-integrity's transcript plan-touch derivation when a manifest is present, and distinguishes "created the state" from "touched the file" so an incidental toucher of a foreign COMPLETED-but-unchecked plan inherits no owner-only remedy. The NL-FINDING-019 golden (scope-line-only touch) now passes with zero waiver language.
- Wiring (Stop chain 6→4) is the orchestrator's §E.W step, not this PR — the dispatcher lands as a present-but-unwired file, same pattern as batches 1–2. Serialization held: zero builder edits to settings.json.template / manifest.json / harness-doctor.sh / install.sh (grep-verified).

## What mechanism would have caught this?

### a) Existing catalog entry

Not a defect fix — this is ADR 059's designed mechanics landing as planned. The relevant standing mechanism is the verify-sequential step: E.11 modifies the three gates that E.4's synthetic scenarios exercise, so the orchestrator re-ran `evals/synthetic/run-all.sh` on the integrated tree (8/8) to confirm the `--report`-mode refactor didn't change the gates' blocking behavior. The builder also self-caught two NL-FINDING-030-class CRLF bugs (jq emits CRLF on Windows, breaking a case-match) during its own build.

## Verification
- Orchestrator re-ran every suite on the integrated tree: work-integrity-gate 43/43 (33 preserved + 7 report + 3 manifest-scoping), session-honesty-gate 37/37 (30 preserved + 7 report), bug-persistence-gate 12/12, stop-verdict-dispatcher 15/15, end-manifest 12/12 — 119 assertions total.
- Golden evals 6/6 PASS; `evals/synthetic/run-all.sh` 8/8 on the integrated tree (the three gates' scenarios still block/pass identically after the --report refactor).
- 019-golden-without-waiver: end-manifest scenario 6 + work-integrity scenario 23 validate clean with zero waiver language.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
