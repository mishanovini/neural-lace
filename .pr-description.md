## Summary

Backlog v65 sync after PR #80 (NL-FINDING-029 hooksPath fixture class) merged as `c1d6dc3`:

- **Last updated** header bumped to v65 recording the shipment.
- New open item **PR-TEMPLATE-CRLF-01** — the NL-FINDING-030 remediation (validator `\r`-strip + run-time body fetch in both template-check CI jobs; needs harness-reviewer; CRLF-body self-test scenario). A separate session is already implementing this.
- **GOLDEN-EVAL-ENV-01** cross-referenced to NL-FINDING-029 as the eval-surface sibling of the same class, with a note that the eval must suppress hooks only on its fixture-setup commit (its assertion needs the pre-push hook live).

Docs-only: the sole content change is `docs/backlog.md`. Rebased forward through batch-2 (#82) and batch-3 (#83) master — backlog auto-merged with zero data loss each time; findings 029/030 already landed via #80.

## What mechanism would have caught this?

### c) No mechanism — accepted residual risk

Docs-only backlog bookkeeping (version-stamp bump, one new tracked item, one cross-reference); there is no defect class here for a mechanism to catch — the substantive classes involved are already ledgered as NL-FINDING-029/-030 on master with their own mechanism proposals in PR #80's template section.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
