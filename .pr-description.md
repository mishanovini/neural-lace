## Summary
Wave-E batch 2: five builder branches merged (build-parallel, verify-sequential per orchestrator-pattern; every branch tip verified on disk against the builder's report and every self-test re-run independently by the orchestrator before merge). One cross-task integration fix applied at the merge boundary (see below).

- **E.3** `scripts/waiver-density.sh` (18/18) — per-gate 7-day waiver count; ≥3 → digest line + idempotent `WAIVER-DENSITY-<gate>-<date>` backlog entry; `--report` mode for E.5. (Live signal it already surfaces: `task-completed-evidence-gate` 5/7d and `work-integrity-gate` 3/7d are over threshold right now.)
- **E.4** synthetic-runner completion (8/8, `deferred.txt` deleted) — 3 new golden scenarios (false-DONE, marker-missing, waiver-abuse) against live Wave-D gates + `Mode: design-skip` companion plan `docs/plans/nl-overhaul-synthetic-ci-2026-07.md` (plan-reviewer clean) + `.github/workflows/synthetic-runner.yml` (weekly cron + hooks/evals PR trigger; first live green run is this PR's checks tab).
- **E.5** `scripts/harness-kpis.sh` (7/7) — weekly KPI report from the ledger (per-gate waiver/downgrade rates, doctor drift, FM recurrence, waiver-density table, nl-issue triage section) + one live report `docs/reviews/harness-kpis-2026-07-03.md`.
- **E.6** `scripts/needs-you.sh` (19/19) — NEEDS-YOU.md ledger (four sections, add/resolve/expire/render/has-entry-for-session), resolves the main-checkout root via nl-paths.
- **E.10** incentive-pin retrofit + defect-fix cluster (all 13 items) — pins d/e/f (block-message contract sweep, new `hooks/lib/waiver-purpose-clause.sh` wired into all waiver-reading gates, economics verdicts in specs-e §E.10-CHECKLIST); findings 017 (install copy-then-verify backup + `check_manifest_freshness`), 019/020 (marker-aware pass-through), 022 (heartbeat WIRE in install.sh + doctor predicate), 023/024/026/027 (session-honesty resolution-aware + registered in `RETRY_GUARD_VERIFICATION_HOOKS`), discovery-2026-06-17 option E (docs/discoveries scope-exempt). Doctor gained 5 checks incl. E.1/E.7/E.8/E.9 predicates; harness-doctor --self-test 23/23, work-integrity 33/33, session-honesty 30/30, install.sh temp-HOME dry-run rc=0.

## What mechanism would have caught this?

### a) Existing catalog entry

The one integration defect this PR fixed — E.4's `scenario-waiver-abuse` breaking against E.10's pin-f change — is exactly the cross-task-interaction class the orchestrator-pattern's verify-sequential step exists to catch: parallel builders build against a fixed base, and the orchestrator re-runs the integrated oracle (here `evals/synthetic/run-all.sh`) after every merge and resolves interactions at the cherry-pick boundary. It surfaced as a synthetic-runner FAIL (7/8) on the integrated tree despite each branch being individually green — precisely what the standing regression floor is for.

## Primary evidence (required for any sweep / class-fix / refactor PR)

### What runtime/log evidence did you pull?

Ran `evals/synthetic/run-all.sh` on the integrated tree after merging all five branches. It returned 7 passed / 1 failed (`scenario-waiver-abuse`), then ran that scenario directly for its stderr.

### What did the evidence show?

`waiver_has_purpose_clauses: command not found` → the honored-waiver case downgraded to `WEAK_WAIVER` instead of `VALID_WAIVER`. Root cause: E.4's scenario extracts `_wig_check_waiver` standalone via sed and sources only that; E.10's pin-f added a call inside it to `waiver_has_purpose_clauses` from the new `lib/waiver-purpose-clause.sh`, which the extracted snippet never sourced. The production hook sources that lib at work-integrity-gate.sh:95 (grep-confirmed), and work-integrity-gate --self-test was already 33/33 — so no production bug, a stale test fixture.

### What hypothesis did you test BEFORE writing the fix?

That mirroring production (source the lib in the scenario) plus giving the "valid" fixture the two pin-f purpose clauses would restore VALID_WAIVER, because pin-f *correctly* downgrades a substantive-but-no-purpose-clause waiver — the old fixture predated pin-f. Confirmed by reading the lib's exact clause regexes before writing the fixtures.

### What refutation criteria would have shown the hypothesis was wrong?

If the honored case had still downgraded after sourcing the lib and adding purpose clauses (would mean pin-f's detection is broken, not the fixture), or if any other scenario regressed. Observed: scenario now 4/4 (added a pin-f abuse assertion: substantive-no-clauses → WEAK_WAIVER), full suite 8/8, golden evals 6/6.

## Verification
- Integrated tree: golden evals 6/6 PASS; `evals/synthetic/run-all.sh` 8/8 (0 deferred); per-builder self-tests re-run green by the orchestrator (E.3 18/18, E.4 8/8, E.5 7/7, E.6 19/19, E.10 gates 33/33 + 30/30 + 23/23 doctor + install dry-run rc=0).
- Serialization held: zero builder edits to settings.json.template or manifest.json (fragments only); E.10 solely owns harness-doctor.sh + install.sh per specs-e §E.0.1.
- Known deferred (not blocking this merge, tracked on the §E.W checklist): doctor --quick against a live mirror will RED-flag manifest-freshness + the E.1↔E.8 nl-issue wiring until §E.W wires fragments + fixes the digest feed; those are wiring steps, not defects in this code.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
