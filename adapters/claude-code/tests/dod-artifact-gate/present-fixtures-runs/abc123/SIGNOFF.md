# Sign-off

Reviewer: self-test fixture
Decision: approved

This is a synthetic SIGNOFF.md file used by `pre-stop-verifier.sh --self-test`.
It contains the literal token "approved" so the requires_pattern check passes,
and it is well over 10 characters so the requires_min_length check passes too.
