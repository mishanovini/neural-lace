# O.3 template-wiring fragment

None. `nl.sh`/`observability-derive.sh` are on-demand CLI/library files,
never registered in `settings.json.template`. Zero new hook entries; zero
removed entries. §O.0.1-8 budget (SessionStart 8/8, Stop 4/6, blocking
gates 10/12) is entirely untouched by this task.
