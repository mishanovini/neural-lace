# Deferred synthetic scenarios — NL Overhaul task E.4 STABLE-SUBSET scaffold
#
# Format: <scenario-basename-without-.sh> — <one-line reason>
#
# These three of the eight golden scenarios named in the parent plan's
# task E.4 line exercise Wave-D gates (session-honesty-gate.sh,
# continuation-enforcer.sh wiring, waiver-density surfaces) that have not
# landed yet. E.4 was dispatched ahead of the D-wave as spec-stable per
# the orchestrator's logged deviation; building red/green fixtures against
# gates that don't exist yet would either fabricate a fixture stub (which
# defeats the point of a golden test) or block this scaffold on Wave D.
# Build these after specs-d lands (per D.2/D.5).

scenario-false-done — tests Wave-D gates; build after D.5 per specs-d
scenario-marker-missing — tests Wave-D gates; build after D.5 per specs-d
scenario-waiver-abuse — tests Wave-D gates; build after D.5 per specs-d
