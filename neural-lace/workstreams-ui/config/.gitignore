# Per-machine project→root map (item 19). Real absolute paths / product
# codenames must NEVER ship in the kit (harness-hygiene). The generic
# projects.example.json + the projects.js resolver ARE tracked.
projects.json
# Per-machine repo map for scripts/work-in-motion-sweep.js (Phase R7). Same
# rule: repo paths / org names / gh accounts never ship; the generic
# wim-repos.example.json IS tracked.
wim-repos.json
# Per-machine completion-oracle CLASS overrides (cockpit-roadmap-redesign
# Task 1, A4). Same rule: the generic completion-oracle.example.json IS
# tracked; a real override file is per-machine config, not kit content.
completion-oracle.json
