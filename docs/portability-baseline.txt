# portability-baseline.txt — the KNOWN-FAILING self-test set.
#
# Generated by: adapters/claude-code/scripts/portability-sweep.sh --write-baseline
# Consumed by : adapters/claude-code/hooks/harness-doctor.sh (check portability-sweep)
#
# The doctor REDs when a script FAILS or TIMES OUT and is NOT listed here.
# Adding a line to this file is therefore an explicit, reviewable act of
# accepting a known-broken suite — not a number quietly edited in a script.
#
# This is a DEBT LIST, not an approval. Every line is a self-test that does
# not pass on the interpreter named below — a real defect someone has to fix.
# Shrinking the list is the work; growing it needs a reason in the commit
# message that adds the line.
#
# Regenerate:
#   bash adapters/claude-code/scripts/portability-sweep.sh \
#        --write-baseline docs/portability-baseline.txt
#
# Regeneration reflects ONE run, so a load-sensitive suite can drop out of it.
# Adding a line by hand is sanctioned — it is exactly the 'explicit, reviewable
# act' this file exists to make visible. Keep the list sorted by path, and say
# in the commit message why the line is there.
#
# Measured: interpreter=/bin/bash  (GNU bash, version 3.2.57(1)-release (arm64-apple-darwin25))
#           roots="hooks hooks/lib scripts"  per-script-timeout=120s  total-budget=5400s
#           discovered=170 pass=124 fail=46 timeout=0 skip=0  (2026-07-29, macos-portability M6)
#
# Format: <STATUS><TAB><path relative to adapters/claude-code>
FAIL	hooks/backlog-plan-atomicity.sh
FAIL	hooks/decision-context-pending-surfacer.sh
FAIL	hooks/decisions-index-gate.sh
FAIL	hooks/docs-freshness-gate.sh
FAIL	hooks/evidence-before-fix-gate.sh
FAIL	hooks/find-disk-scan-gate.sh
FAIL	hooks/harness-doctor.sh
FAIL	hooks/lib/observability-derive.sh
FAIL	hooks/lib/perf-tick-snapshot.sh
FAIL	hooks/migration-claude-md-gate.sh
FAIL	hooks/plan-edit-validator.sh
FAIL	hooks/plan-lifecycle.sh
FAIL	hooks/plan-reviewer.sh
FAIL	hooks/pr-template-inline-gate.sh
FAIL	hooks/propagation-trigger-router.sh
FAIL	hooks/review-finding-fix-gate.sh
FAIL	hooks/session-start-digest.sh
FAIL	hooks/session-start-git-freshness.sh
FAIL	hooks/workstreams-emit.sh
FAIL	hooks/workstreams-read.sh
FAIL	scripts/ask-registry.sh
FAIL	scripts/check-cross-repo-drift.sh
FAIL	scripts/close-plan.sh
# HAND-ADDED 2026-07-29 (M5). Not in the generating run: coord-sync.sh's suite is
# LOAD-SENSITIVE, not consistently broken. Observed across 6 full sweeps on this
# machine: passed 4, failed 2 (both times while other agents were saturating the
# box), and failed twice in a row once — which the runner's retry-before-
# regression cannot absorb. Baselined so it cannot produce a false RED. Remove
# this line together with the fix that makes the suite load-independent.
FAIL	scripts/coord-sync.sh
FAIL	scripts/ensure-cockpit.sh
FAIL	scripts/estate-brief.sh
FAIL	scripts/estate-janitor.sh
FAIL	scripts/f4-retro.sh
FAIL	scripts/gate-demotion.sh
FAIL	scripts/harness-eval-decision-queue-bridge.sh
FAIL	scripts/master-drift-autocorrect.sh
FAIL	scripts/model-availability.sh
FAIL	scripts/nl.sh
FAIL	scripts/pr-health-snapshot.sh
FAIL	scripts/remap-placeholder-ask-events.sh
FAIL	scripts/scheduled-task-health.sh
FAIL	scripts/session-wrap.sh
FAIL	scripts/start-plan.sh
FAIL	scripts/state-summary.sh
FAIL	scripts/static-trace.sh
FAIL	scripts/supervisor-tick.sh
FAIL	scripts/waiver-density.sh
FAIL	scripts/worktree-hygiene-sweep.sh
FAIL	scripts/write-evidence.sh
FAIL	scripts/write-review-record.sh
