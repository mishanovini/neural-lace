---
title: v1.1.2 item 25 was pre-shipped as item 22 by a parallel v1.1.1 superset PR
date: 2026-05-18
type: scope-expansion
status: decided
auto_applied: true
originating_context: docs/plans/conv-tree-ui-v1.1.2-polish.md — merge of origin/master into branch conv-tree-ui-v1.1.2-polish
decision_needed: n/a — auto-applied (reversible feature-branch reconciliation)
predicted_downstream:
  - docs/plans/conv-tree-ui-v1.1.2-polish.md
  - neural-lace/conversation-tree-ui/web/app.js
  - neural-lace/conversation-tree-ui/web/app.css
  - neural-lace/conversation-tree-ui/web/index.html
  - neural-lace/conversation-tree-ui/web/responsive.selftest.js
---

## What was discovered

v1.1.2 (items 25–28) was speced by Misha and implemented against the
items-14-18 v1.1.1 base (PR #10, master `301a5b7`) — the v1.1.1 that had
merged when this session started. A SECOND, larger v1.1.1 effort merged in
parallel during this session: **PR #11 "conv-tree-ui v1.1.1 — items 14-23"**
(branch `claude/jolly-davinci-d99487`, merged 2026-05-18 18:52), a superset
that landed master at `759923d`.

PR #11's **item 22** already shipped the exact filled-semantic-button system
v1.1.2 item 25 specifies: classes `btn-go`/`btn-wait`/`btn-info`/`btn-up`/
`btn-del`/`btn-neutral` with CSS vars `--btn-go:#22c55e` … `--btn-neutral:#475569`
— the identical palette Misha gave for item 25 — and it reclassified all 23
action buttons in `app.js`. Item 25 is therefore **already delivered on
master**. PR #11 also added a docs viewer (item 19), restructured the
`index.html` header, and rewrote `responsive.selftest.js` for items 14-23.

Items 26, 27, 28 are confirmed NOT covered by the items-14-23 superset
(master still has the `renderActions()`-rebuild det-toggle, the quiet
`it.kind==='action' ? 'mark done' : 'mark answered'` button, and the ISO
`prompt()` defer). Item 28's backend (`item-backlogged` event + optional
`deferred` local-time fields + P16) merged into master's `state/*`
**without conflict** — intact.

## Why it matters

Shipping v1.1.2's `.b-*` button system on top of master's already-merged
`btn-*` system would put TWO competing semantic-button systems in one file —
the exact mess to avoid. v1.1.2's real net-new content is items 26–28 (plus
adopting the existing `btn-*` for any button those items touch). Misha's
mental model ("rebase onto v1.1.1 = items 14-18") was invalidated by the
parallel superset; he needs to know item 25 was already done so the v1.1.2
PR is not misrepresented as delivering it.

## Options

A. Drop the redundant `.b-*` item-25 implementation; adopt master's
   `btn-*`; re-apply ONLY items 26–28 on master's base. (Reversible —
   feature-branch reconciliation, one revert.)
B. Keep `.b-*` and rename master's `btn-*` to it (rejected — rewrites
   already-merged, already-verified item-22 work; gratuitous churn).
C. Abandon v1.1.2, fold 26–28 into a fresh plan off `759923d` (rejected —
   the work is done and verified; reconciliation is cheaper than re-plan).

## Recommendation

A. The engineering call is unambiguous (never ship two button systems);
   the reconciliation is reversible; the user explicitly anticipated a
   "rebase once v1.1.1 lands" and authorized autonomous drive-to-merge.

## Decision

A — auto-applied. Reversible (all on the `conv-tree-ui-v1.1.2-polish`
feature branch; a single `git revert`/branch-reset undoes it). Surfaced
prominently to Misha in the session's final report per friction-reflexion.

## Implementation log

- Merge `origin/master` (759923d) into branch — conflicts in app.js +
  index.html; app.css + responsive.selftest.js auto-merged; state/* clean.
- app.js / index.html: take master's items-14-23 base; re-apply items
  26/27/28 only, using master's `btn-*` classes where touched.
- app.css: remove the redundant `.b-*` block (keep `.defer-pop` for 28).
- responsive.selftest.js: rework the appended block to cover 26/27/28 +
  the additive `item-backlogged` against master's structure + `btn-*`.
- Plan + Decisions Log updated to record item 25 = delivered by merged
  item 22; re-verify; finish merge + PR #12.
