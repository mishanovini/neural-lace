# Decisions Index

Permanent record of Tier 2+ architectural and product decisions for the Neural
Lace harness. Every entry below points at a standalone record in
`docs/decisions/NNN-*.md` with full Context, Decision, Alternatives, and
Consequences. Adding a new record requires updating this index in the same
commit (enforced by `adapters/claude-code/hooks/decisions-index-gate.sh`).

| # | Title | Date | Status |
|---|-------|------|--------|
| 001 | [Fresh orphan-commit for public release](decisions/001-public-release-strategy.md) | 2026-04-18 | Active |
| 002 | [Attribution-only anonymization policy](decisions/002-attribution-only-anonymization.md) | 2026-04-18 | Active |
| 003 | [`review-before-deploy` as automation-mode default](decisions/003-review-before-deploy-default.md) | 2026-04-18 | Active |
| 004 | [Capture-codify mechanism field shape](decisions/004-capture-codify-mechanism-field-shape.md) | 2026-04-23 | Implemented |
| 005 | [40-character rationale threshold for "no mechanism" answer form](decisions/005-capture-codify-rationale-threshold.md) | 2026-04-23 | Implemented |
| 006 | [CI workflow + local pre-push hook (both layers)](decisions/006-capture-codify-ci-and-local-hook.md) | 2026-04-23 | Implemented |
| 007 | [Per-repo opt-in for the pre-push PR template hook](decisions/007-capture-codify-per-repo-hook-optin.md) | 2026-04-23 | Implemented |
| 008 | [Failure-modes file as a stub created by capture-codify plan](decisions/008-capture-codify-failure-modes-stub.md) | 2026-04-23 | Implemented |
| 009 | [Do not change repo squash-merge commit-message setting](decisions/009-capture-codify-squash-merge-body.md) | 2026-04-23 | Implemented |
| 010 | [Validator library lives at `.github/scripts/` (not `adapters/claude-code/`)](decisions/010-capture-codify-validator-library-location.md) | 2026-04-23 | Implemented |
| 011 | [Claude `--remote` harness portability — Approach A (commit harness into project) + Routines + DevContainers, Dispatch out of scope](decisions/011-claude-remote-harness-approach.md) | 2026-04-23 | Implemented |
| 012 | [Agent Teams integration — six design decisions](decisions/012-agent-teams-integration.md) | 2026-04-27 | Active |
| 013 | [Default git push policy — auto-push (safe methods); customer-tier branching for real-user projects](decisions/013-default-push-policy.md) | 2026-05-03 | Active |
| 014 | [Calibration mimicry design — RL-shaped via prompt conditioning (G-1 through G-4)](decisions/014-calibration-mimicry-design.md) | 2026-05-03 | Active |
| 015 | [PRD-validity gate (C1): single `docs/prd.md` per project + 7 required sections + harness-development carve-out](decisions/015-prd-validity-gate-c1.md) | 2026-05-04 | Active |
| 016 | [Spec-freeze gate (C2): `frozen: true|false` semantics, freeze-by-commit-SHA, freeze-thaw protocol](decisions/016-spec-freeze-gate-c2.md) | 2026-05-04 | Active |
| 017 | [Plan-header schema locked: 5 required fields, no defaults, gated on `Status: ACTIVE`](decisions/017-plan-header-schema-locked.md) | 2026-05-04 | Active |
| 018 | [Spec-section divergence from SCRATCHPAD: Build Doctrine §6 chosen as authoritative; `## Provides`/`## Consumes`/`## Dependencies` deferred](decisions/018-spec-section-divergence-from-scratchpad.md) | 2026-05-04 | Open |
| 019 | [Findings-ledger format (C9): 6-field schema, single `docs/findings.md` per project, dispositioning lifecycle](decisions/019-findings-ledger-format.md) | 2026-05-04 | Active |
| 020 | [Comprehension-gate semantics (C15): rung-2 cutoff, four articulation fields, FAIL/INCOMPLETE blocks task-verifier](decisions/020-comprehension-gate-semantics.md) | 2026-05-04 | Active |
| 021 | [DRIFT-02 resolution: SessionStart account-switching hook is config-driven](decisions/021-drift-02-account-switch-config-driven.md) | 2026-05-04 | Active |
| 022 | [`pipeline-agents.md` deleted from global rules](decisions/022-pipeline-agents-md-deletion.md) | 2026-05-04 | Implemented |
| 023 | [Definition-on-first-use enforcement: acronym regex + stopword allowlist + scope-prefix + glossary/in-diff semantics](decisions/023-definition-on-first-use-enforcement.md) | 2026-05-04 | Active |
| 024 | [GAP-14 template-vs-live reconciliation outcomes (six per-hook verdicts; all REVERSIBLE; auto-applied)](decisions/024-gap-14-reconciliation.md) | 2026-05-04 | Implemented |
| 025 | [Build Doctrine same-repo placement: `build-doctrine/` and `build-doctrine-templates/` as sibling top-level directories in NL, rejecting the original three-repo architecture](decisions/025-build-doctrine-same-repo-placement.md) | 2026-05-05 | Active |
| 026 | [Harness catches up to doctrine: when the harness diverges from the Build Doctrine, the harness is the side that catches up; reactive enforcement paused; Tranche 1.5 of the roadmap operationalizes this](decisions/026-harness-catches-up-to-doctrine.md) | 2026-05-05 | Active |
| 027 | [Autonomous decision-making process: pre-emptive decision queues at plan kickoff + autonomous proceed on reversible mid-execution decisions + mandatory ADR documentation + final-summary surfacing](decisions/027-autonomous-decision-making-process.md) | 2026-05-05 | Active |
| 028 | [`session-wrap.sh` falls back to parent repo's SCRATCHPAD when run from a worktree](decisions/028-session-wrap-worktree-fallback.md) | 2026-05-09 | Active |
| 029 | [Local-edit authorization mechanism (skill + hook + per-file marker) replaces broad `permissions.deny` on `~/.claude/local/**`](decisions/029-local-edit-authorization-mechanism.md) | 2026-05-09 | Active |
| 030 | [`scope-enforcement-gate.sh` union-of-plans approach for merge commits — DEFERRED behind the shipped lightweight migration-allowlist; ADR captures the more-general design for future implementation](decisions/030-scope-enforcement-gate-merge-aware-union-of-plans.md) | 2026-05-14 | Deferred |
| 031 | [Conversation Tree Management UI architecture — r7 ACCEPTED: Option 2 file-mediated state contract, passive observer GUI. Forward-compat (versioned schema, append-only log+snapshot, well-known path, schema contract doc). Cloud blind spot accepted. Enforcement: hooks DO fire on Dispatch orchestrator → spawn-time + Stop gates (freshness/shape/branch-name mechanical; semantic + cloud rule-backed)](decisions/031-conversation-tree-ui-architecture.md) | 2026-05-16 | Accepted |
| 032 | [Conversation Tree state schema — JSON field-layout contract: int major `schema_version`, append-only `events` + derived `snapshot` w/ coverage marker, finalized event-type enum + idempotency `event_id`, node shape + strict-tree invariant, FR-2 conversational-divergence cardinality (N=3 fixture), {decision,question,action} item set (defer is a state not a type), one-file-per-tree path resolution + cross-tree links w/o multi-parenting, NFR-2 conflict-unit = single event record, Pin-3 properties restated as fixed inputs, enforcement-shaped layout (branch-presence = single `jq` key check)](decisions/032-conversation-tree-state-schema.md) | 2026-05-17 | Active |
| 033 | [Failure-Mode catalog is a cross-project convention on the existing canonical `docs/failure-modes.md` six-field schema, additively extended with two optional fields (Discriminator, Recovery); forked-directory alternative rejected; investigation-first grep reflex added; auto-search proposed not shipped](decisions/033-failure-mode-catalog-cross-project-convention.md) | 2026-05-19 | Active |
| 034 | [Conversation-tree gates scoped to Dispatch spawn tools only — sub-agent `Task`/`Agent` removed from the enumerated set (AI-internal mechanics, not user↔AI conversation branches). Amends ADR-031 r7 Pin 1 → r8. Narrows all 3 conv-tree hooks + 2 settings matchers; removes the daily spurious-waiver friction on parallel sub-agent dispatch; de-pollutes the operator tree](decisions/034-conversation-tree-scope-dispatch-only.md) | 2026-05-19 | Active |
| 035 | [Diagnostic-First Protocol + Hypothesis-vs-Proof Labeling + Refutation-Criteria Requirement — three Pattern-class rules under `~/.claude/rules/` motivated by 8+ days of FM-001 misdiagnosis on inferential evidence; pulls runtime logs as the FIRST tool call of any production-failure investigation; tags every causal claim PROVEN/HYPOTHESIZED; pairs every hypothesis with a refutation criterion before authoring an implementation plan on top of it](decisions/035-diagnostic-first-protocol.md) | 2026-05-22 | Active |
| 036 | [Plan Lifecycle: mechanical closure machine attached at CREATION — four locked sub-decisions: (a) acceptance scenarios mandatory + populated before ACTIVE (gate makes mandatory, generator removes friction); (b) PASS-artifact contract (commands/expected-output/on-disk-location/done-when) defined at creation in a `## Closure Contract` section; (c) AUTO-closure — plan auto-flips COMPLETED + archives when last task verifies AND contract artifact exists (`close-plan.sh` becomes the abandonment-only path); (d) staleness prevented by mandatory `owner:` + `target-completion-date:` commitments with a narrow commitment-breach decision moment — NOT auto-defer. Design-only; implementation gated on Misha](decisions/036-plan-lifecycle-mechanical-closure.md) | 2026-05-25 | Proposed |
| 037 | [Canonical file-lifecycle policy for session-generated artifacts (Pattern 3 of 5) — three-class taxonomy (ephemeral / durable-deliverable / tool-vs-instance) backs three mechanisms: (D2) `session-wrap.sh` SCRATCHPAD create-on-missing-from-template fixes the `1666666 min` stale-sentinel Stop-loop (RC1); (D3) `docs-publish-on-stop.sh` copy-if-absent + staging-ledger backstop propagates worktree-written durable docs to the main checkout (RC2; write-time path-redirect rejected as hook-infeasible); (D4) track the reusable tool, retire the dated instance (RC3). Design-only; implementation gated on Misha](decisions/037-file-lifecycle-session-artifacts.md) | 2026-05-25 | Proposed |
| 038 | [Pending-items marker convention + auto-extraction into the Conversation Tree (RC4) — a narrow written contract (`**Questions/Action items/Decisions for Misha**` anchored headers + 4 list shapes) backs `conversation-tree-extract-pending.sh`, a Stop hook that scans the final assistant message and pipes items to the existing `conversation-tree-emit.sh --emit-item`/`--emit-branch` modes (shipped 2026-05-21, supersedes the stranded 2026-05-20 reimplement-appendEvent sketch). Retires the manual `add-pending-items.js`. Design-only; implementation gated on Misha](decisions/038-pending-items-marker-convention.md) | 2026-05-25 | Proposed |
| 039 | [Conversation-tree visibility: reconciliation over interception (Pattern 5 of 5) — emit-hook matcher CONFIRMED CORRECT (brief's `mcp__dispatch__*` premise refuted; namespace is `mcp__ccd_session__*`); the hook's never-firing is a structural blind-spot (PreToolUse only fires when a hook-loading local session *calls* the tool — GUI/cloud spawns bypass it) + a cwd state-path divergence (`neural-lace/neural-lace` doubling, claude-projects vs dev-checkout split). Adopt a `list_sessions` SessionStart reconciler + a cwd-independent canonical state path as the PRIMARY visibility mechanism; doubles as the RC4 duplicate-spawn detector. ADR-034 matcher scope unchanged. Design-only; build gated on Misha](decisions/039-conv-tree-reconciliation-over-interception.md) | 2026-05-25 | Proposed |
| 040 | [Session Resilience: three-layer curability model (Layer 1 tool-input = PreToolUse-curable; Layer 2 context-growth = measure+act-while-alive; Layer 3 API-transport = NOT hook-curable, platform-owned) + four locked sub-decisions: (a) the layer model is the spine + whole-design refutation criterion; (b) dispatch pre-flight guard BLOCKS oversized subagent prompts, never auto-trims (silent truncation = false promise); (c) Bash retry wrapper retries idempotent commands only, irreversible-op denylist (git commit/push, gh merge, migrations), API-transport retry OUT (platform-owned); (d) topic-shift = advisory surface, transcript-size ceiling = mechanical hard enforce — hard-enforce only where the signal is reliable; (e) handoff WRITE always automatic via PostToolUse heartbeat (bounds loss of ANY death incl. transport), RESPAWN auto in spawnable modes / surface-and-resume in interactive. Cures FM-030..033 (Pattern 4 of 5). Design-only; implementation gated on Misha](decisions/040-session-resilience-three-layer-model.md) | 2026-05-25 | Proposed |
| 041 | [Dispatch-mode auto-detection signal (Pattern 5 of 5) — SessionStart hook publishes `dispatch-mode.json` via a priority ladder (`CLAUDE_CODE_DISPATCH` override → manual-lock honored → `CLAUDE_CODE_ENTRYPOINT=claude-desktop` + agent-SDK corroboration → default false). Brief's proposed `CLAUDE_CODE_PROVIDER_MANAGED_BY_HOST` signal REFUTED (absent on CC 2.1.146); conservative over-flag of desktop sessions only downgrades MC-widget→plain-text (safe fallback). Provenance recorded in `detected_by`. Design-only; build gated on Misha](decisions/041-dispatch-mode-autodetect-signal.md) | 2026-05-25 | Proposed |
| 042 | [ntfy.sh out-of-band notification contract (Pattern 5 of 5) — opt-in `ntfy-notify.sh` POSTs (≤5s, no-content payload, topic-in-local-layer per harness-hygiene) on `Notification` (default) / `Stop` (opt-in); routes a child's needs-input around a sleeping Dispatch parent to Misha's phone. Curative for the human-awareness leg, explicitly NOT for parent-wake (RC1, Anthropic-blocked). Absent config → silent no-op. Design-only; build gated on Misha](decisions/042-ntfy-out-of-band-notification.md) | 2026-05-25 | Proposed |
| 043 | [Decision Queue substrate (renumbered from 036 during the 2026-05-28 NL-unification cutover — collided with the existing ADR-036 Plan-Lifecycle mechanical-closure on PT) — persistent prioritized queue at `~/.claude/state/decision-queue/` for asks the human owes; schema + shell storage script + Dispatch calling-convention doc + harness-evaluator integration; v1 priority-score formula (age + dependents + highlight + aging-tax); Mechanism = storage + schema, Pattern = when-to-add + when-to-highlight; Conv Tree panel implementation deferred to follow-up session (spec doc ships in this PR)](decisions/043-decision-queue-substrate.md) | 2026-05-24 | Active |
| 044 | [Neural Lace cross-repo mirror automation — a single parameterized GitHub Action (`mirror-to-sister.yml`) deployed to both repos mirrors each master to the sister at identical SHAs on push; loop-broken by SHA-equality + a `merge-base --is-ancestor` benign-no-op check; FF-only never force; non-FF (true divergence or merge-commit-vs-linear-history) fails loud + ntfy, no auto-resolve. Token model: two fine-grained PATs (Contents:write on the sister only), identity in per-repo Actions vars not committed YAML, bypass-allowance on the protected master. `sync.sh` rewritten URL-based (push to each distinct remote URL, name-independent, fail-loud). Staged-not-enabled — enabling gates on the unification cutover (Q1 SHA reconciliation first). 043 reserved for the decision-queue ADR (personal PR #36 renumber)](decisions/044-neural-lace-mirror-automation.md) | 2026-05-27 | Reverted 2026-05-28 (superseded by harness-internal drift detection; sync.sh kept) |
| 045 | [Conversation Tree → Workstreams work-first reframe — rename the subsystem (`conversation-tree-ui` → `workstreams-ui`) + reinterpret the SAME ADR-031/032 substrate work-first (additive only: 3 lifecycle event types item-committed/shipped/blocked + optional tier/serves_item_id on branch-opened; parent_id broadened to Project/Workstream/WorkItem; schema_version stays 1). Renderer rebuilt to a four-tier Project→Workstream→WorkItem→Sub-task hierarchy with focus-project default expansion, sessions demoted to detail-card provenance, a filter-driven side panel (default = non-complete work) replacing the stacked accordion, a detail card, and an adjustable divider. Phase 1+2 shipped; hook-filename rename (Task 2b) + Phases 3-6 (Agent View integration, hard-block orphan gate, autonomous reconciler, cross-machine sync) follow](decisions/045-workstreams-reframe.md) | 2026-06-01 | Accepted |
| 046 | [Workstreams Phase 3 — lifecycle emit + backfill + hook-filename rename. `--on-spawn` parses a `Work-item:` sentinel → emits `serves_item_id` + `session-bound` (+ kind event for the `new` form); `--on-stop` emits `item-shipped` (evidence=HEAD SHA) on commit detection, ordered before `concluded` (FR-7), best-effort/no-false-ship. `lifecycle-backfill.js` stamps `committed` on parked items only (in-flight/proposed stay render-derived; shipped not stamped to avoid false recent-ship ts) — 62 events applied. Task 2b: `conversation-tree-*.sh` → `workstreams-*.sh` via git mv + delegating shims (not symlinks; Windows-safe) + settings/rule/INDEX/arch refs; runtime state paths unchanged; verified non-breaking via both names. Phase 4 orphan/recently-shipped windows made localStorage-configurable. Personal-mirror sync deferred (fork-reconciliation)](decisions/046-workstreams-lifecycle-emit.md) | 2026-06-01 | Accepted |
| 047 | [Decision-Context Gate Enforcement Surface — Stop-hook reactive model chosen over Pre-SendUserMessage-hook (the imagined surface that does not exist; `mcp__ccd_session__send_user_message` is not exposed, per the conv-tree auto-emit precedent that deferred wrapping it). The new `decision-context-gate.sh` scans the last assistant message in `$TRANSCRIPT_PATH` at Stop; Tier-1 triggers (enumerated options / terminal `?` + list / explicit "pick one"/"your call"/"which do you want") without a fence BLOCK Stop with the Zod schema as the error → agent redoes as fenced message; Tier-2 weaker signals → no block + follow-up marker; Tier-3 rhetorical ("does that make sense?") → whitelisted no-op. Closes the verbal-vaporware sub-case of `vaporware-prevention.md` "Residual gap (honest)" for decision-soliciting prose. Pre-filter regex bounds cost (node/Zod only invoked when trigger present). Migrates Layer D of `conv-tree-orchestrator-emit.md` from Pattern-only to Mechanism+Pattern. Sibling-pattern precedent: six shipped Stop-hook BLOCK-with-redo gates (`continuation-enforcer`, `narrate-and-wait-gate`, `goal-coverage-on-stop`, `deferral-counter`, `imperative-evidence-linker`, `principles-compliance-gate`); inherits the shared `stop-hook-retry-guard.sh` 3-retry loop-break. Composes cleanly with ADR-031 r7/r8, ADR-032 §8 r2.1, ADR-034 (uses `state.js` facade + attestation primitive; orthogonal to Dispatch-only spawn-side scoping). Cost accepted: message-lands-once-before-redo (the reactive surface trade); `node`-subprocess on trigger-fire (pre-filter mitigated); fires on every Stop universally (intrinsic to surface)](decisions/047-decision-context-enforcement-surface.md) | 2026-05-30 | Active |
| 048 | [Auto-install harness changes on SessionStart — `session-start-auto-install.sh` (wired first in the SessionStart `""` matcher) continuously, surgically syncs live `~/.claude/hooks/` + `~/.claude/scripts/` from the freshest *fetched* `origin/master` ref (`git show`, not the working tree — branch-independent, sidesteps the install footgun), master-wins-with-backup for executable surfaces, additive validate-before-atomic-swap jq-merge for `settings.json`. Compares content modulo CRLF/LF to avoid install.sh ping-pong. Idempotent (verified live: run 1 = 19 installed + 78 updated; run 2 = no-op), exits 0 always, 13-scenario self-test. Closes the cross-machine propagation gap (a merged hook reaches every machine's live dir on next session, no per-machine install.sh). v1 scopes to executable surfaces; rules/agents/docs widening + prune deferred to v2](decisions/048-auto-install-on-session-start.md) | 2026-06-02 | Accepted |
| 049 | [Feature-completion criteria gate — eight criteria (code/tests/dev-docs/user-docs/migration/deploy/acceptance/stakeholder) moved from social to mechanical via a `completion-criteria-gate.sh` Stop hook (block-mode default, wired after `pr-health-snapshot-gate`); fires only on feature-shipment phrasing (NOT the bare session-end `DONE:` marker — that's session-end-protocol's job), then requires a `## Completion Criteria` section accounting for all 8 as ✓+evidence or N/A+justification; escape hatches `COMPLETION_GATE_SKIP=<criteria>` (per-criterion, audit-logged) + `COMPLETION_GATE_DISABLE=1` + warn-mode; routes block through `stop-hook-retry-guard.sh` (unmet-set signature → fixing some resets the counter). Paired with `page-doc-accuracy-audit.sh` — Part B, redesigned 2026-06-01 (per Misha) from a backwards-facing completed-plan walker to a FORWARD-FACING page-vs-doc accuracy audit: for every live contractor-facing page (PageRegistry `owner:'org'`) it statically checks the support doc against the page's current UI — 🔴 STALE (doc names UI absent from all of `src/`; high-precision via a prebuilt source index so mapping errors can't false-flag; exit-blocking), 🟡 UNDOCUMENTED (page heading the doc never mentions), ⚪ MISSING_DOC. The plan-walker is removed. Two layers, both forward-facing: the gate catches incompleteness at session-close, the audit catches drift on already-shipped pages. gh-free + node-free self-tests (gate 14/14, audit 14/14) → no KNOWN_FAILING_HOOKS entry. Closes the systematic "merged = shipped" gap (F4/C-47/Smart-Import/What's-New)](decisions/049-feature-completion-criteria-gate.md) | 2026-06-01 | Active |
| 050 | [orchestrator-prime — the always-on harness-native orchestrator (/loop architecture). A self-scheduling Code session (ScheduleWakeup/CronCreate) polls a file-mediated inbox + conversation tree + sibling-session status, spawns/tracks work, surfaces via outbox, emits tree events — replacing Dispatch's orchestration role (Dispatch demoted to thin relay). Built strictly to the verified tool surface: the original transcript-read parent-wake relay is REFUTED (RC1 — no inter-session send/resume/read primitive; only list_sessions/search_session_transcripts/archive_session exist), so /loop self-wake + file inbox sidesteps it; "revive rate-limited child" = detect+surface+respawn-with-ack (no message-revive); first cycle report-only; customer-repo merges surfaced not auto-merged (git.md tier policy). Inbox/outbox Dispatch seam UNVERIFIED → smoke-tested before launch, fallback = conv-tree GUI + ntfy. Brief called it "ADR-068"; correct sequential number is 050. Composes with ADR-039/041/042](decisions/050-orchestrator-prime-loop-architecture.md) | 2026-06-02 | Proposed |
| 052 | [DEFERRED plans go to `docs/plans/deferred/`, not `docs/plans/archive/` — the lifecycle split the destination by status: COMPLETED/ABANDONED/SUPERSEDED → archive/ (done-with), DEFERRED → deferred/ (intended-but-not-currently-active, the plan-level backlog). Fixes the conflation of terminal-for-editing vs done-for-building that buried still-intended roadmap. Mechanized in `plan-lifecycle.sh` (PostToolUse) + `plan-status-archival-sweep.sh` (SessionStart safety net), each self-test-locked; `find-plan-file.sh` searches active→deferred→archive. Re-activate = flip back to ACTIVE (git mv back); truly-drop = flip to ABANDONED (archives). Originating: Misha 2026-06-04 stale-plan cleanup. ADR 051 reserved by cross-machine-coordination](decisions/052-deferred-plans-not-archived.md) | 2026-06-04 | Accepted |
| 053 | [Customer-Facing Review Gate — mechanical UX + customer-advocate enforcement. New `customer-facing-review-gate.sh` Stop hook (block-mode default, mirrors `pr-health-snapshot-gate.sh`) scans the agent-uneditable `$TRANSCRIPT_PATH` for spawn surfaces (`mcp__ccd_session__spawn_task` / `mcp__ccd_session_mgmt__start_code_task` / `Agent` builder dispatches), classifies each spawn blob customer-facing (STRONG contractor signals override platform/backend EXCLUSION; WEAK fire only absent exclusion), and BLOCKS session wrap when a customer-facing spawn was made without BOTH a UX-family agent (`ux-designer`/`UX End-User Tester`/`Domain Expert Tester`/`Audience Content Reviewer`) AND `end-user-advocate`. Closes the 2026-06-02 failure where the orchestrator spawned 4 customer-facing sessions (Nav IA, Smart Import v2, doc-reviewer, support-backfill) with zero UX/CX review — moving the planning.md/testing.md prose mandate from SOCIAL to MECHANICAL. Escape hatches `[skip-ux-review: <reason>]` + `UX_REVIEW_GATE_DISABLE=1` (audit-logged, reason mandatory); 3-retry downgrade via `lib/stop-hook-retry-guard.sh`; 8-named-scenario `--self-test`. Adds the 9th completion-criterion handed off to the in-flight completion-criteria-gate via `.claude/state/spawned-task-results/`. Spawn-keyed (direct-edit customer-facing work not caught — prose mandate + operator authority cover it). Chosen over a pre-spawn payload-rewrite hook (enforces outcome-presence, not process-ordering) and over leaving it prose (the status quo that failed 4×)](decisions/053-customer-facing-review-gate.md) | 2026-06-02 | Active |
| 054 | [Builder-dispatch work-item emission + tracking-substrate gate — amends ADR-034 for the WORK-ITEM tier (branch scoping stands): every orchestrator builder dispatch (Task|Agent|Workflow) auto-emits an idempotent action-added work-item (details._category=builder-dispatch — never Awaiting-me; derives in-flight) on the session ss-* node via `workstreams-emit.sh --on-builder-dispatch` (PreToolUse) with `--on-builder-complete` (PostToolUse) emitting action-done for FOREGROUND dispatches only; background launches (Workflow / run_in_background) get creation-only — NO stable completion contract exists locally (named Rule-7 ceiling, items stay honestly in-flight; FR-7 keeps their session un-concludable). Stop-time reconciler sweep catch-up-fires both modes from the agent-uneditable transcript (tool_result presence = foreground completion). Forcing-first/gate-second: thin `workstreams-state-gate.sh --builder-tracking` BLOCKS a dispatch when tracking is possible (node+state-lib present) but the canonical state file is missing/unwritable; degrades OPEN when the subsystem is absent (bootstrap rule); waiver valve + disable env. Driver: Misha 2026-06-10 — tracking must happen automatically, mechanically, even when the agent does not want to listen. Self-tests: emit 52, gate 27, reconciler 9](decisions/054-builder-dispatch-work-item-emission.md) | 2026-06-10 | Active |
| 055 | [Workstreams UI re-conceived as a shared status surface — keep the event-sourced foundation, re-conceive the presentation (Option C of the 2026-06-11 discovery). Density principle: COUNTS globally (per-project cockpit rows, O(projects)), ITEMS only in one bounded slice (waiting list / one drilled tree). Five surfaces: cockpit · waiting-on-you · per-project drill tree (color=STATUS, icon=KIND, amber=needs-you ONLY) · editable My-tasks · editable Backlog with promote. Operator authoring reuses the existing event vocabulary (`action-added`+derived persisted `origin`, `item-text-set`, `reordered`, `backlog-activated`); exactly ONE new event `item-removed`, additive within ADR-032 major 1. Context-completeness ships as gate + contract: render-side completeness derived by the sole-normative `assembleItemDetails` (fail-closed; incomplete items get NO resolving buttons), emit-side `workstreams-emit.sh` carries per-kind payloads as `item-details-set` (absent→honest detail-less WARN, never blocks). All 8 `window.prompt` sites retired; one overlay-dismiss stack; renameSync+event_id idempotency, no mutex claim. 11/11 tasks verified, 4/4 runtime acceptance scenarios PASS. Residuals: WS-UI-FOLLOWUPS-01](decisions/055-workstreams-status-surface.md) | 2026-06-12 | Implemented |
| 056 | [Workstreams deploy-detection + builder-dispatch bucketing fix — the :7733 GUI showed Deployed=0 forever (NO hook emits `item-deployed`; only the manual GUI button set it) and a 211-item shipped-not-deployed backlog of which 93 were AI-internal completed builder dispatches. Two additive ground-truth fixes, no schema bump / no new hook / no hand-edit: (1) `work-in-motion-sweep.js` gains a `collectDeploys(repo)` collector reading production ground truth from the operator's authenticated Vercel CLI (`vercel ls --prod`; table parsed from stderr; Windows `.cmd` + `VERCEL_BIN` handled) and emits `item-deployed` for shipped `wim-pr-*`/`wim-br-*` nodes whose ship predates the latest Ready prod deploy — conservative (only merged-code categories; no Ready deploy → stays shipped-not-deployed; deploy SKIP on no-`.vercel`/unauth → never a false deploy; idempotent). (2) `web/app.js` `isShippedNotDeployed` excludes `details._category==='builder-dispatch'` (211→118 immediately; completed dispatches fall through to Recently-shipped). Auto-resolving stale awaiting-me asks REJECTED on honesty grounds — surfaced as an operator-controlled UI proposal instead. wim-sweep self-test 49/49.](decisions/056-workstreams-deploy-detection-and-builder-dispatch-bucketing.md) | 2026-06-17 | Active |
| 058 | [The Great Consolidation — NL overhaul architecture executing DEC-2026-07-01-001 (operator greenlit the full program 2026-07-02). Nine locked decisions: D1 always-loaded constitution ≤~6K tokens (CLAUDE.md ≤100 lines + constitution.md ≤350 lines; everything else leaves the auto-load dir); D2 just-in-time doctrine via ONE config-driven injector (`doctrine-jit.sh`, PostToolUse path-patterns, per-session dedup) + gate block-messages as the delivery surface; D3 single machine-readable `manifest.json` (kills the 3×-recorded rule→hook map ~131KB); D4 `harness-doctor.sh` keystone (claimed-vs-actual wiring verified mechanically; --quick <2s at SessionStart, --full in CI/weekly); D5 gate consolidation to budgets Stop ≤6 / SessionStart ≤8 / blocking ≤12 — artifact checks block, narrative checks demoted to ledger observations, one marker contract (continuation-enforcer semantics finally live inside session-honesty-gate); D6 one signal ledger + one ≤15-line digest replacing 12 surfacers + waiver-density alarm + HARNESS_SELFTEST sandboxing; D7 compliance telemetry (synthetic-session-runner golden scenarios + weekly KPIs; new blocking gates require named scenario + FP expectation + retirement condition); D8 per-task model tiering (haiku sweeps / sonnet hooks / opus design+cutovers) via the Agent/Workflow model param; D9 wave execution ≤4-5 parallel builders + wave-spec JIT refinement + rollback tags + attic retention. Program-level refutation criteria included (synthetic scores must improve + waiver/downgrade rates must drop ≥50%, else pause — not re-sprawl). Evidence: docs/reviews/2026-07-01-neural-lace-effectiveness-audit.md; companion plan docs/plans/nl-overhaul-program-2026-07.md](decisions/058-nl-overhaul-great-consolidation.md) | 2026-07-02 | Active |
| 057 | [Worktree-Isolation Enforcement — two mechanisms make per-session git-worktree isolation real without false-firing on the many cases it's wrong for. `session-start-worktree-advisor.sh` (SessionStart) auto-injects tailored guidance — the START side a hook CANNOT force (cwd is chosen before SessionStart fires) so it only informs: silent in a worktree/non-git, LOUD on the main checkout of a repo that has worktrees, gentle otherwise; every message names the create command + setup cost + exemption set. `worktree-teardown-gate.sh` (Stop, after product-acceptance) blocks ending inside a linked worktree with UNCOMMITTED work, steering preserve-first (commit/stash/push) and NEVER `--force` (honors "incomplete ≠ abandoned", DEC-B1); clean-but-unpushed → non-blocking advise (DEC-D2); cwd-scoped so it can't disturb a peer's worktree (no liveness signal — DEC-B2; created-then-cd-away orphan a named v1 limitation); main-checkout/non-git/locked exempt; fresh-waiver + retry-guard mirror bug-persistence-gate. Operator asked to identify every non-wanted case BEFORE building (bad exemptions erode gate trust). Self-tests: advisor 4/4, teardown 8/8. Supersedes the Pattern-only 2026-06-22 teardown spec](decisions/057-worktree-isolation-enforcement.md) | 2026-06-23 | Active |

<!-- 2026-05-25: ADRs 037 + 038 added (NEW; Pattern 3 of 5, file-lifecycle-redesign session, sibling to ADR 036's plan-closure). 037 = file-lifecycle policy (3-class taxonomy → RC1 session-wrap create-on-missing / RC2 worktree-publish-on-stop / RC3 track-tool-retire-instance; write-time path-redirect rejected as PreToolUse-hook-infeasible). 038 = pending-items marker convention + extraction Stop hook reusing the 2026-05-21 --emit-item modes. Both Proposed (design-only; implementation gated on Misha + 3 open decisions in docs/plans/file-lifecycle-redesign.md). Originating diagnosis: docs/discoveries/2026-05-25-file-lifecycle-root-cause-chain.md. -->
<!-- 2026-05-04: Records 001, 002, 013 received in-place codename scrub (Phase 1d-G Task 1). Content unchanged in substance; identifiers replaced with generic placeholders to satisfy harness-hygiene scanner. No status changes. -->
<!-- 2026-05-05 v3: ADR 027 extended with Layer 5 (handoff freshness as precondition). No row change to the index — same row 027 covers v1 + v2 + v3. -->
<!-- 2026-05-06: ADR 027 Layer 5 signal-set extended from 5 to 6 (added stale-pointer detection in What's Next). No row change. -->
<!-- 2026-05-15: ADR 031 hardened to r2 after systems-designer Phase-3 FAIL (4 findings addressed by honest restatement; Option-4 recommendation unchanged). Status stays Proposed pending Misha's decision. No row change. -->
<!-- 2026-05-15: ADR 031 r3 (path-resolution note) — OQ-9/Decision-A resolved to canonical docs/prd.md (forced by prd-validity-gate + gate-respect; PRD git-mv'd from docs/prd-conversation-tree.md). No row change. -->
<!-- 2026-05-15: ADR 031 r3 (architecture rework) — stakeholder STRUCK Option 4 during interactive PRD Stage-B; option space now 1/2/3; "built into Dispatch" added as hard input (not buildable by us → approximate-the-ideal framing); Option 1 ("Dispatch++") recommended-pending; obsolete r2 per-field-mergeable pin reopened; Phase-3 hardening section marked HISTORICAL; r3 needs a fresh systems-designer pass before adoption. Row 031 title updated to match. -->
<!-- 2026-05-16: ADR 031 r4 — Misha (Stage C) reopened all three options; r3's Option-1 lean DOWNGRADED to "leading candidate, not settled"; overlay/observer posture is NOT disqualifying (Misha frames the product as an abstraction layer on top of Dispatch). Decision stays open to the post-Stage-F architecture pass. Row title updated. -->
<!-- 2026-05-16: ADR 031 r5 — research corrected a load-bearing premise (Dispatch is LOCAL-desktop execution, not cloud; r2–r4 cloud-disqualification of Options 2/3 retracted) + struck the unbuildable "launch real Dispatch" variant (external software cannot launch Dispatch) + added Option 1b (GUI spawns its own Agent-SDK Claude Code children, Dispatch not in execution path) + reopened Options 2/3 on the corrected local premise + separated Option 2 (passive observe+relay) from Option 3 (active Channels command). Decision axis now: own-the-surface (1/1b) vs overlay-on-local-Dispatch (2/3). Still Misha's call + pending systems-designer pass. Row title updated. -->
<!-- 2026-05-16: ADR 031 r6 — Misha scope clarification (GUI is a PASSIVE tracker; Dispatch stays primary chat; not a chat replacement) COLLAPSED the decision → Option 2 (passive local-transcript observer) SELECTED. Options 1/1b/3 rejected-with-rationale as over-scoped. 2026-05-16 readability research verified on Misha's machine that Dispatch sessions write standard local transcripts; one open risk = cloud-only sessions (web/--remote/Routines) have no local file. FR-6 + FR-24 reinterpretations feed a PRD-revision diff shown to Misha before commit. Still pending systems-designer pass + Misha final ack before adoption. Status → Proposed (Option 2). Row title updated. -->
<!-- 2026-05-16: ADR 031 r7 — Misha ACCEPTED ("I think this Dispatch GUI is acceptable. it sounds like the only reasonable option"). Status Proposed→Accepted. Added `## Adopted decision (r7)`: file-mediated state contract + forward-compat refinements (versioned schema / append-only event-log + snapshot / well-known path / written schema contract doc) + cloud blind-spot acceptance + enforcement design from the 2026-05-16 hooks-research (hooks DO fire on Dispatch orchestrator → conversation-tree-state-gate.sh PreToolUse on spawn + conversation-tree-stop-gate.sh Stop; mechanical = freshness/shape/branch-name; rule-class = semantic correctness + cloud path via Decision 011). FR-6/FR-24 diff + cascade committed (1b1cbc6). systems-designer Tier-5 adversarial pass returned FAIL with 3 "pin-the-property-now" findings (spawn-path coverage enumeration; fail-closed bounded-recovery + error-partition; append-log torn-snapshot/atomicity/compaction) — accepted option stood, all 3 pins folded into `## Adopted decision (r7) → Plan-safety pins`. **Architecture-pick phase COMPLETE.** Plan-and-build is a separate phase requiring Misha's explicit greenlight after he sees this adopted record. -->
<!-- 2026-05-17: ADR 032 r1 (DEC-D — §7c/§8 cross-clause gap closure) — resolves NL-FINDING-003 (the §7c compaction emptied events[] while §8 reads events[]-only → silent Phase-B spawn DoS on long-lived trees). Misha DECIDED option (b), generalized: §7c revised to encode the GENERAL principle "compaction may not drop any event that is still live and gate-relevant" (branch-opened-per-still-live-node = v1's only instance); §8 left structurally unchanged (torn-snapshot-immune events[]-only design preserved) + an explicit "§7c↔§8 — CLOSED, not worked around" clause; rejected options a (§8 reads snapshot) / c (§8 audit fallback). No schema_version bump (additive/behavioral within major 1 — no required field changes, §8 query byte-identical). Implemented by plan task B-DEC-D (store.js generalized retention predicate + selftest P9/P10; findings NL-FINDING-003 → closed). Status stays Active; no row title change (the contract description still holds — this constrains the writer's compaction behavior, it does not change the field layout). -->
<!-- 2026-05-17: ADR 032 r2 (DEC-D = option (d) snapshot-integrity attestation — SUPERSEDES r1's option (b)) — Misha, after r1, raised a deeper architecture question and confirmed option (d): the §7c↔§8 gap (NL-FINDING-003) is resolved by a snapshot-integrity ATTESTATION PRIMITIVE, not a compaction carve-out. The r1 (b) code (91c86f8: gateRelevantRetention + r1 §7c text + selftest P9/P10) FAILED both plan-mandated reviews with a severe §7a regression (NL-FINDING-004) and is REMOVED. (d): every snapshot write atomically appends a {type:"snapshot-committed", hash:"sha256:<canonical-JSON digest>", at} record to events[] in the SAME write-temp-then-rename publish; §7c reverts to ORIGINAL A2 truncate-covered-prefix (NO per-gate carve-out, ever); §8 (and any future gate) verifies the snapshot by canonical-hashing it vs the most-recent snapshot-committed.hash, then reads snapshot.nodes — mismatch ⇒ refuse + §7a torn-recovery; facade gains attestSnapshot()/verifySnapshotAttested(); stated as a GENERAL primitive (architectural principle: no per-gate compaction carve-outs). Dissolves NL-FINDING-004 + moots DEC-E/DEC-F (re-opened/promoted/backlog-activated nodes already in snapshot.nodes). No schema_version bump (snapshot-committed is an additive new event class; reducer forward-tolerant; no required-field/marker-contract change — per the §1 major-bump rule). Implemented by plan task B-DEC-D-(d) (store.js original compaction + attestation; state.js facade; selftest P9–P13, 13/13 PASS; A2 P1–P8 + Phase-0 3-step regression PASS). Status stays Active; no row title change. The §180 dangling citation flagged by the r1 review is fixed → "§1 major-bump rule". -->
<!-- 2026-05-17: ADR 032 r2.1 (corrective amendment to r2 §8; NO architecture change) — systems-designer FAILed r2 on one defect (code-reviewer concurred): the r2 §8 verify-then-read bullet presented a shell shape `jq -cS '.snapshot' | sha256sum` and CLAIMED it was the "shell-equivalent / byte-identical to canonicalJSON". Empirically FALSE (sha256sum hashes jq's trailing newline; jq -cS number/unicode formatting differs from JS canonicalJSON → digest never matches the writer's snapshot-committed.hash; a Phase-B gate built faithfully from §8 would BLOCK 100% of legitimate spawns — orchestrator-DoS class DEC-D dissolves, relocated into the verifier; same false-signal shape as NL-FINDING-004). r2 selftest green only because P9–P13 exercise the in-process Node verifier, never the §8 shell path. Fix (DEC-D rule 4 already mandates the library primitive as canonical — no architecture change): §8 makes state-library verifySnapshotAttested/hashSnapshot the SOLE NORMATIVE verifier (gate runs `node -e` requiring the module against the on-disk file); the jq snippet retained ONLY as explicitly NON-NORMATIVE illustrative shape with a hard "NOT hash-equivalent / MUST NOT compute the trust hash" caveat; determinism rebound to single implementation (one store.canonicalJSON, writer + every verifier). New selftest P14 spawns a REAL `node` subprocess (the exact sanctioned shape) proving untampered⇒verified+digest-match, byte-tampered⇒not-verified. No store.js/state.js change (in-process verifier already correct per code-reviewer). Full suite 14/14; A2 P1–P8 + Phase-0 3-step regression re-run PASS. Implemented by plan task B-DEC-D (r2.1 corrective). Status stays Active; no row title change (a §8-text correction, not a contract-field change). -->
<!-- 2026-05-19: ADR 031 r8 (amends r7 Pin 1) — the conversation-tree enumerated spawn set is narrowed from four tools to the two Dispatch tools (mcp__ccd_session__spawn_task | mcp__ccd_session_mgmt__start_code_task). Sub-agent Task/Agent removed: they are AI-internal mechanics (peer review/verification/parallel builders), NOT branches of the user↔AI conversation the tree models. r7 Pin 1's "Task/Agent must not be a silent gap" reasoning was wrong for the tree's purpose — gating/emitting on them polluted the operator tree and forced daily spurious per-session waivers (3-of-4 parallel reviewers blocked ×2 sessions + verification + plan-reviewer Task, all one day). Full decision in ADR-034. r7 Pin 1 text retained verbatim under Tier-5 option-history discipline, read as superseded. Row 031 status → "Accepted (r7, amended r8)". -->
<!-- 2026-05-19: ADR 034 (NEW; amends ADR-031 r7 Pin 1 → r8) — Conversation-tree gates scoped to Dispatch spawn tools only. Narrows all 3 conv-tree hooks (conversation-tree-state-gate.sh, conversation-tree-stop-gate.sh, conversation-tree-emit.sh --on-spawn) + the 2 settings.json.template conv-tree PreToolUse matchers from the 4-tool set to mcp__ccd_session__spawn_task|mcp__ccd_session_mgmt__start_code_task. Task/Agent deliberately out of scope (scoping decision, not an accepted gap). Bash(claude…)//schedule gap unchanged; ADR-032 §8 attestation/branch-name bar unchanged (smaller matcher). Self-tests extended with sub-agent-not-gated regressions: state-gate 20/20, stop-gate 9/9, emit 17/17. Status Active. -->


