# Findings ledger

This file records class-aware observations made by gates, adversarial-review agents, and builders during work on Neural Lace itself. Per Decision 019 (`docs/decisions/019-findings-ledger-format.md`), every entry follows the six-field schema. The schema gate (`adapters/claude-code/hooks/findings-ledger-schema-gate.sh`) validates each entry on commit; the rule documenting when and how to write entries is `adapters/claude-code/rules/findings-ledger.md`; the canonical template is `adapters/claude-code/templates/findings-template.md`.

## Schema specification

| Field | Required | Type | Valid values |
|---|---|---|---|
| `ID` | Yes | string | Project-prefixed kebab-case identifier (e.g., `NL-FINDING-001`). Unique within `docs/findings.md`. |
| `Severity` | Yes | enum | `info`, `warn`, `error`, `severe` |
| `Scope` | Yes | enum | `unit`, `spec`, `canon`, `cross-repo` |
| `Source` | Yes | string | Names which gate / agent / role surfaced the entry. |
| `Location` | Yes | string | `file:line` reference, artifact path, or `n/a` if process-shaped. |
| `Status` | Yes | enum | `open`, `in-progress`, `dispositioned-act`, `dispositioned-defer`, `dispositioned-accept`, `closed` |

The `Description` body field is required substantive content explaining the observation in enough detail that a future-session reader can understand it without re-deriving the context.

## Entries

### NL-FINDING-001 — plan-reviewer.sh Check 1 + Check 7 false-positives on meta-plans

- **Severity:** warn
- **Scope:** unit
- **Source:** orchestrator (manual observation during Phase 1d-C-2 plan-review pass; corroborated by Phase 1d-C-2 plan-builder return)
- **Location:** adapters/claude-code/hooks/plan-reviewer.sh — Check 1 (undecomposed sweep regex on Definition of Done plural language) and Check 7 (design-mode shallowness regex on legitimate concise sections)
- **Status:** dispositioned-defer
- **Description:** When plan-reviewer.sh runs against meta-plans (plans about the harness itself, not project features), Check 1 trips on plural language in `## Definition of Done` ("all scenarios", "every task") and Check 7 trips on the word `table` in design-mode sections referring to Markdown tables rather than database tables. Workaround: rephrase plan content to avoid the regex hits. Mitigation deferred per HARNESS-GAP-09 (P3 — workaround is trivial; not blocking). To act: tighten the Check 1 regex to NOT fire on lines under `## Definition of Done`, and Check 5 regex to be context-aware (database-context vs documentation-context). Estimated effort: ~30 minutes.

### NL-FINDING-002 — Sub-agent background tasks and polling loops leak past sub-agent completion

- **Severity:** warn
- **Scope:** unit
- **Source:** orchestrator (observed 2026-05-04 in this session arc; user surfaced via UI screenshot of ~21 "Running" tasks; OS-level confirmation via `ps -ef`)
- **Location:** Pattern, not a single file. Affects any sub-agent that uses Bash `run_in_background: true` or Monitor. Cleanup gap is in Claude Code's task-state machine, not in user code.
- **Status:** open
- **Description:** During this session arc, sub-agents (plan-phase-builders + task-verifiers + audit agents) spawned background bash tasks for self-tests, polling loops (`until ...; do sleep 5; done`), and CI-watch invocations. After each sub-agent returned, two distinct leak patterns observed: (1) **OS-level zombies** — at least one `until` polling loop on `gh pr checks 3` was found alive 15 hours past its parent sub-agent's completion (PID 475, killed manually). (2) **Task-tracker zombies** — the Claude Code task panel showed ~21 "Running" entries while OS-level `ps` showed only 1 actual process. Most "Running" entries had no corresponding OS process; their tracking metadata was stale, not their underlying work. Practical effects: (a) genuinely-stuck `until` loops poll forever (one cycle per 5 seconds is ~17K cycles in 24 hours; cumulatively wastes CPU + API quota for `gh` calls), (b) the task panel becomes unreadable as zombie entries accumulate session-arc-over-session-arc, and (c) future sub-agents and the `bug-persistence-gate.sh` may misinterpret zombie task records as in-flight work. Mitigation candidates: (i) sub-agents should explicitly `TaskStop` any background tasks they spawn before returning (rule + builder-prompt update), (ii) Claude Code's task tracker should auto-transition tasks whose owning sub-agent has terminated (upstream change), (iii) periodic OS-level sweep of zombie polling loops as a SessionStart housekeeping hook (mechanism). Cross-reference: `~/.claude/rules/orchestrator-pattern.md` (parallel-builder protocol — does not currently document the cleanup obligation).

### NL-FINDING-003 — automation-mode-gate.sh resolves project config only from $PWD/.claude, blinding worktrees branched before the config commit (HARNESS-GAP-37)

- **Severity:** warn
- **Scope:** unit
- **Source:** orchestrator (operator-directed harness-friction triage, 2026-05-17 — GAP-2)
- **Location:** adapters/claude-code/hooks/automation-mode-gate.sh (config resolution in `_run_gate`, formerly only `$PWD/.claude/automation-mode.json`)
- **Status:** closed
- **Description:** `automation-mode-gate.sh` resolved the per-project automation mode solely from `$PWD/.claude/automation-mode.json`. A git worktree whose branch was created from a master that PREDATES the project's `full-auto` config commit has no such file in its own working tree, so the probe missed and the gate silently fell back to the user-global `review-before-deploy` default — then correctly-but-confusingly BLOCKED deploy-class Bash commands (`git push`, `gh pr merge`, `vercel deploy`) in a project that IS configured `full-auto`. Every Dispatch worktree of a `full-auto` project that branched before the config landed paid this friction. Fix: when the `$PWD/.claude/` probe misses AND the cwd is a git worktree (`git --git-dir` ≠ `--git-common-dir`), resolve the parent checkout via `dirname(git-common-dir)` and read its committed-and-checked-out `.claude/automation-mode.json` — the same parent-resolution ADR 028 applied to `session-wrap.sh`'s SCRATCHPAD lookup. The parent checkout is the canonical project root and carries the config regardless of how old the worktree branch is. Verified by a new self-test Scenario 5 (positive: PASS from a worktree branched pre-config because the parent's full-auto is resolved; negative control: BLOCK once the parent config is removed, proving the PASS came from the fallback). 5/5 self-test scenarios PASS; mirror synced byte-identical.

### NL-FINDING-004 — session-wrap.sh tracked-file freshness signals read the parent checkout, structurally unclearable from a worktree (HARNESS-GAP-38)

- **Severity:** warn
- **Scope:** unit
- **Source:** orchestrator (operator-directed harness-friction triage, 2026-05-17 — GAP-3)
- **Location:** adapters/claude-code/scripts/session-wrap.sh (`cmd_verify` Signals 3/4/5 + `plans_touched_this_session`, formerly all keyed to `find_repo_root` = parent)
- **Status:** closed
- **Description:** `session-wrap.sh`'s `find_repo_root()` resolves to the PARENT repo when run from a worktree (ADR 028 — correct for SCRATCHPAD, which is gitignored ephemeral state, one per repo). But Signals 3 (roadmap), 4 (discoveries) and 5 (backlog) read TRACKED, version-controlled files that a worktree session legitimately edits in its OWN worktree and ships via PR; the parent checkout's copy is correctly untouched until that PR merges. Reading the parent's copy made the agent's correct action (edit the worktree copy, open a PR) structurally unable to clear the freshness signal — the check reads the other checkout. Fix: added `find_worktree_root()` (returns `git rev-parse --show-toplevel` = the worktree's own toplevel, or the repo root when not a worktree); `cmd_verify`/`cmd_refresh` now take an optional second `wt_repo` arg (defaults to `repo` so direct callers and non-worktree sessions are byte-for-byte unchanged); the tracked-file signals + `plans_touched_this_session` read `wt_repo`, SCRATCHPAD signals keep reading the parent. New self-test S9a (worktree's fresh backlog → PASS, proving the fix) + S9b (genuinely stale worktree backlog → STALE, proving the fix does NOT mask real staleness — it just reads the right copy). S1–S8 unchanged (no regression); 10/10 PASS; mirror synced. **Boundary:** this is orthogonal to and does NOT resolve the still-pending discovery `2026-05-17-session-wrap-signal3-transitive-false-fire.md` — that is a separate root cause (the global 4h `--since` window mis-attributes WHICH COMMITS count) awaiting Misha's decision; this finding fixes WHICH CHECKOUT's copy is read. The two compose cleanly.

### NL-FINDING-005 — chronic per-session acceptance waivers on a stale unstarted ACTIVE plan (downstream-project instance) (HARNESS-GAP-36)

- **Severity:** warn
- **Scope:** cross-repo
- **Source:** orchestrator (operator-directed harness-friction triage, 2026-05-17 — GAP-1)
- **Location:** a downstream pre-customer project — `docs/plans/prd-v1.1-and-audit-resolution.md` (Status: ACTIVE, 0/7 tasks); ~5 acceptance waivers on 2026-05-17
- **Status:** dispositioned-act
- **Description:** Diagnosis (per the operator's GAP-1 ask: incomplete vs complete-but-not-flipped vs gate-over-firing): **genuinely incomplete**. The plan was filed 2026-05-14 ("file PRD-v1.1 resolution plan") but never started — all 7 task checkboxes `- [ ]`, Evidence Log empty, no completion report, every Definition-of-Done item unchecked, and Task 5's partial-UNIQUE-index migration is absent from the project's migrations dir. `product-acceptance-gate.sh` is therefore firing CORRECTLY (a real, unstarted, non-exempt `Status: ACTIVE` plan with no PASS artifact); the per-session waivers are the CORRECT behavior for unrelated sessions. The defect is not the gate — it is that real product work sat unstarted for 3 days while every session in that project paid the waiver tax (its SCRATCHPAD itself notes "11 stale Status: ACTIVE plans force per-session acceptance waivers"). Resolution (operator pre-authorized "if incomplete: spawn the dedicated session"): a dedicated downstream build session was spawned via `mcp__ccd_session__spawn_task` to drive all 7 tasks to task-verifier PASS + flip `Status: COMPLETED` (which stops the waiver tax at its root). **This is a downstream-project instance of the harness-wide plan-staleness class already tracked by HARNESS-GAP-29/30/31** (DoD-saturation surfacer, all-checked-but-ACTIVE detector, waiver-density alarm). The broader observation (≥11 stale ACTIVE plans in that project) is the GAP-29/30/31 aggregation gap manifesting concretely, not a new class — recorded here as the trigger instance; the systemic fix remains GAP-29/30/31.
### NL-FINDING-006 — ADR-032 §7c compaction empties events[] but §8 enforcement reads events[] (Phase-B spawn DoS on long-lived trees)
- **Renumbered at merge:** was NL-FINDING-003 on the conversation-tree-ui-v1 branch; renumbered 006 to resolve a parallel-allocation ID collision with master's harness-friction NL-FINDING-003 (both closed; conv-tree plan/evidence prose retains original 003/004 wording as historical record).
- **Severity:** error
- **Scope:** canon
- **Source:** code-reviewer (Task A2 plan-mandated review, 2026-05-17)
- **Location:** docs/decisions/032-conversation-tree-state-schema.md §7c↔§8; neural-lace/conversation-tree-ui/state/store.js:226-235
- **Status:** closed
- **CLOSED 2026-05-17:** resolved by DEC-D = option (d) snapshot-integrity attestation, task-verifier PASS (conf 9) at branch tip `0b4ac5d`. Resolving commits: `19bb3fc` ((d) impl, REPLACED the failed (b) `91c86f8`) + `8b77035` (§8 r2.1 corrective, cherry→`4ae6f46`). §8 no longer reads `events[]` for branch-presence — it verifies the snapshot via the most-recent `snapshot-committed` attestation hash then reads `snapshot.nodes`; the §7c↔§8 gap structurally cannot recur. systems-designer FAIL(19bb3fc)→PASS(r2.1) + code-reviewer PASS + comprehension-reviewer rung-2 PASS; 14/14 selftest incl. P14 real-subprocess digest-equality. The `ADR-032 line 180` citation in this finding's body below is superseded — the rule is `ADR-032 §1 major-bump rule`.
- **RE-OPENED then DISPOSITIONED 2026-05-17:** the B-DEC-D (b) attempt (`91c86f8`) was prematurely self-marked closed but FAILED both plan-mandated reviews (severe §7a regression — NL-FINDING-004); reverted closed → in-progress. Misha then raised a deeper architecture question and **decided DEC-D = option (d) snapshot-integrity attestation** (supersedes (b); see plan Decisions Log → DEC-D for the binding 5-point rule). Under (d) this finding is resolved by an *attestation primitive*: §8 no longer reads `events[]` for branch-presence — it reads the snapshot AFTER verifying it against the most-recent `snapshot-committed` hash in `events[]`; compaction stays original-behavior (no carve-out). Status → dispositioned-act (decided action = the B-DEC-D-(d) rework). Finalizes to `closed` only when the (d) rework passes systems-designer + code-reviewer + comprehension + task-verifier. The B-DEC-D checkbox was correctly NEVER flipped.
- **Description:** ADR-032 §7c compaction (faithfully implemented by A2 at store.js:226-235) truncates `events[]` to `[]` once a fresh snapshot provably covers all events. But §8 specifies the Phase-B `conversation-tree-state-gate.sh` branch-presence check as `jq -e '.events[] | select(.type=="branch-opened" and (.title==$b or .node_id==$b))'` — run against `.events[]` (explicitly NOT the snapshot, with the stated rationale "so a torn snapshot never weakens the gate"). After compaction on a long-lived tree (FR-24 "a minute or a month"), `branch-opened` records live ONLY in `snapshot.nodes` → the §8 jq filter returns non-zero → the Phase-B gate would BLOCK every subsequent legitimate Dispatch spawn until the user writes a waiver: a silent DoS against the orchestrator on exactly the long-running trees the feature targets. A2's code is contract-faithful and is NOT changed unilaterally (that would itself be an un-surfaced contract deviation); this is a §7c↔§8 cross-clause contract gap requiring an ADR-032 revision (ADR-032 line 180: a frozen-clause-touching change needs the ADR-revision path, not a silent in-flight edit). Candidate resolutions for the revision decision (DEC-D, surfaced to Misha): (a) §8 gate also checks `.snapshot.nodes[]`; (b) compaction retains the most-recent `branch-opened` per still-live node in `events[]` (preserves both §7c bound and §8-against-events[] torn-snapshot-immunity — recommended); (c) §8 gate falls back to the never-truncated audit log. **Blocks Phase B** (Phase B builds the §8 gate; it must not be built against the broken interaction). Class: contract-interaction-gap-not-surfaced (a frozen-contract clause whose interaction with a sibling clause produces a downstream-phase failure). Sweep query: `rg -n 'covers_through_event_id|publishedEvents = \[\]|\.events\[\]|jq -e' neural-lace/conversation-tree-ui/ docs/decisions/032-conversation-tree-state-schema.md` — review every ADR-032 cross-section field-consumption (§7a↔§7c, §7c↔§8, §6↔§2) for the same literal-text-correct-but-cross-clause-breaking shape. **Resolution (2026-05-17, lifecycle open → dispositioned-act → closed):** resolved by plan task B-DEC-D per Misha's DECIDED DEC-D = option (b), generalized. ADR-032 §7c revised to encode the *general* principle "compaction may not drop any event that is still live and gate-relevant" (branch-opened-per-still-live-node = v1's only instance); §8 left structurally unchanged (its torn-snapshot-immune events[]-only design preserved) with an explicit `§7c↔§8 — CLOSED` clause; ADR-032 r1 revision note added (no schema_version bump — additive/behavioral within major 1). `store.js` compaction now retains the gate-relevant-still-live set via a generalized `gateRelevantRetention()` predicate (registry-driven so a future gate inherits the rule with one entry). `selftest.js` extended with P9 (post-compaction the exact §8 events[]-only jq branch-presence filter resolves every still-live branch — the Phase-B spawn DoS is closed; archived-node branch-opened correctly NOT retained) and P10 (retained events[] bounded by still-live-node count — not 0, not full history). Full suite 10/10 PASS (P1–P8 regression intact); Phase-0 Walking-Skeleton 3-step regression PASS. Resolving commit: `91c86f8` (B-DEC-D, cherry-picked onto claude/kind-faraday-c5fe05).

### NL-FINDING-007 — B-DEC-D §7c retention regresses §7a: compaction marker points past the published subset → silent tree-state loss on long-lived trees
- **Renumbered at merge:** was NL-FINDING-004 on the conversation-tree-ui-v1 branch; renumbered 007 (same parallel-allocation collision; both closed).
- **Severity:** severe
- **Scope:** canon
- **Source:** systems-designer + code-reviewer (Task B-DEC-D plan-mandated reviews, 2026-05-17)
- **Location:** docs/decisions/032-conversation-tree-state-schema.md §7a↔§7c; neural-lace/conversation-tree-ui/state/store.js:296-307 (compaction marker) vs store.js:121-133 (readState marker check)
- **Status:** closed
- **CLOSED 2026-05-17:** dissolved by DEC-D = option (d) (task-verifier PASS, tip `0b4ac5d`; commits `19bb3fc`+`8b77035`). The (b) `gateRelevantRetention` code that caused this §7a marker-vs-published-subset regression was fully REMOVED; compaction restored to original A2 behavior; §8 trusts the attestation-verified snapshot, not the coverage marker — so this regression structurally cannot arise. Proven by selftest P12 (the exact NL-FINDING-004 FR-24 trace now preserves items/checked/draft/concluded post-compaction) + P13 (attestation survives repeated compaction) + P14 (real-subprocess digest-equality). DEC-E/DEC-F confirmed MOOT under (d) (§8 reads `snapshot.nodes` which already contains re-opened/promoted/backlog-activated — proven by P10). The §180→§1 hygiene item was folded into the §8 r2.1 fix (ADR-032 now cites `§1 major-bump rule`).
- **DISPOSITION 2026-05-17 — (b) approach ABANDONED; superseded by DEC-D = option (d):** Misha decided DEC-D = option (d) snapshot-integrity attestation (see plan Decisions Log → DEC-D). The (d) rework REMOVES the (b) `gateRelevantRetention` code that caused this regression and restores the original A2 compaction; §8 reads the attestation-VERIFIED snapshot, never the marker-vs-published-subset of events[]. Therefore this regression structurally cannot arise under (d) — the marker-vs-published invariant the (b) attempt broke no longer exists (compaction is original-behavior; trust comes from the `snapshot-committed` hash, not the coverage marker). DEC-E and DEC-F are MOOT under (d) (§8 reads `snapshot.nodes` which already contains re-opened/promoted/backlog-activated nodes). Status → dispositioned-act (decided action = the B-DEC-D-(d) rework, which replaces `91c86f8`'s (b) code). The (d) selftest explicitly includes this finding's FR-24 trace as a regression proof. Finalizes to `closed` when the (d) rework passes both reviews + task-verifier. The non-blocking hygiene items (§180→§1 citation; tie "intact" claims to selftests) are folded into B-DEC-Da.
- **Description:** The B-DEC-D fix attempt (`91c86f8`) for NL-FINDING-003 closes the narrow §8 branch-presence DoS (selftest P9 proves the events[]-only jq still resolves live branch-opened post-compaction) but introduces a STRICTLY-WORSE Pin-3a regression, caught by both plan-mandated reviewers (independent FAILs). Root: compaction sets `snapshot.covers_through_event_id` = the FULL log's last event_id (store.js:296) while publishing `events[]` = the gate-relevant-retained subset (store.js:306) whose last element is generally an EARLIER event. The §7a reader marker check `markerOk = snap.valid && snap.covers_through_event_id === events[last].event_id` (store.js:121-133) then evaluates FALSE on every realistic post-compaction read whose final pre-compaction event is not a retained branch-opened — so the reader DISCARDS the valid on-disk snapshot and re-derives state from the lossy 1-event-per-live-node subset, silently destroying every checklist item, answered/done state, draft (FR-27), conclusion, cross-link, and backlog entry compaction removed. Empirically reproduced (7-event FR-24 tree: n1.items 3→0; concluded→open; draft lost) — general state-data loss on exactly the long-lived trees DEC-D exists to protect. The 10/10 selftest is a FALSE signal: P3/P9/P10 fixtures use only single-node bare branch-opened events and structurally cannot exercise non-branch-opened-final-event state. Class: cross-clause-invariant-broken-by-revision (a contract revision closing one cross-clause gap by producing a state-file shape that violates a sibling clause's correctness precondition — the SAME shape as NL-FINDING-003, recurred §7a↔§7c one clause over; the NL-FINDING-003 sweep was not re-run for the sibling pair). Converged minimal fix (both reviewers): at compaction set `snapshot.covers_through_event_id` = the LAST PUBLISHED (retained) event's id (`publishedEvents[publishedEvents.length-1].event_id`, or null if empty), NOT the full log's last; align §7a prose + the store.js readState marker semantics; delete/correct the false "§7a unchanged / Pin-3a intact" claims in ADR-032 §7c/§8/r1-note/DECISIONS.md + this finding's predecessor note; add selftest P11 (non-branch-opened-final-event FR-24 trace asserting post-compaction readState() preserves items/checked-states/drafts/conclusions); run + document the §7a↔§7c class-sweep (`rg -n 'covers_through_event_id|markerOk|publishedEvents|gateRelevantRetention|lastId' store.js docs/decisions/032-*.md`) per diagnosis.md. Two SECONDARY findings from the same reviews, requiring contract-intent DECISIONS (surfaced to Misha as DEC-E/DEC-F — NOT synthesized): (DEC-E, error) archive→compact→re-open still reproduces the NL-FINDING-003 DoS — `gateRelevantRetention` drops an archived node's branch-opened at compaction, but reducer.js:116-121 `re-opened` revives archived nodes unconditionally; contract must decide either (a) reducer guards `re-opened` to only reverse `concluded` (archived is truly terminal) or (b) retention treats archived as revivable — this touches the §7c "still-live" definition. (DEC-F, warn) the retention registry covers only `branch-opened`, but `promoted` (reducer.js:182) and `backlog-activated` (reducer.js:206) also create live nodes the §8 `node_id` clause can be queried for (backlog-activated's §2 spec literally says "Emits the equivalent of a branch-opened"); contract must decide whether v1 §8 spawns can be named by a promoted/backlog node_id (extend `GATE_RELEVANT_EVENT_CLASSES`) or explicitly scope them out in §7c/§8. Non-blocking hygiene also flagged: ADR-032/findings dangling absolute "§180/line 180" citation should anchor to "§1 major-bump rule"; every "intact/unchanged/Pin-X preserved" claim must be tied to a citing selftest (the original false-confidence shape). **Blocks Phase B** (B1's §8 gate must be built against a correct, non-regressing §7c/§8 — the corrected B-DEC-D rework, gated on DEC-E/DEC-F, must land first). NL-FINDING-003 reverted to in-progress accordingly.

### NL-FINDING-008 — Dispatch conv-tree emitter v1 concludes a branch on the dispatching session's Stop, not on precise child-DONE
- **Severity:** info
- **Scope:** unit
- **Source:** plan-author (dispatch-conv-tree-event-emission plan, Decisions Log)
- **Location:** adapters/claude-code/hooks/conversation-tree-emit.sh (--on-stop); docs/plans/dispatch-conv-tree-event-emission.md
- **Status:** dispositioned-defer
- **Description:** `conversation-tree-emit.sh --on-spawn` emits `branch-opened` for a spawned child branch and records it to a per-session ledger; `--on-stop` emits `concluded` for every branch the *dispatching* session opened. v1 therefore concludes a spawn branch when the session that dispatched it ends — NOT at the precise moment the spawned child reports DONE. Reliable cross-session child-DONE correlation needs a shared key in both the parent spawn `tool_input` and the child session (the parent's `title` ≠ the child's first user message, so no deterministic hash matches). The `spawn-task-report-back.md` `Report-back: task-id=<X>` sentinel convention is the correct v2 correlation key. **Disposition: defer to v2** — the v1 behavior satisfies the feature intent (open branches stay surfaced per FR-24; they conclude when the dispatching conversation wraps) and all four prescribed verification steps; precise child-DONE is a refinement, not a correctness gap. v2: have `--on-spawn` inject/parse the `Report-back: task-id=` sentinel and conclude on the spawned-task-result surfacer's ack rather than the parent Stop. Class: cross-session-correlation-deferred. Sweep query: `rg -n 'Report-back: task-id=|spawn-task-report-back' ~/.claude/rules ~/.claude/hooks`.

### NL-FINDING-009 — conv-tree emitter resolved the GUI sink via the worktree git root, not the main checkout the operator's single GUI server watches (found + fixed in same change)
- **Severity:** error
- **Scope:** unit
- **Source:** functionality-verifier (live end-to-end against the running :7733 GUI, dispatch-conv-tree-event-emission Task 5)
- **Location:** adapters/claude-code/hooks/conversation-tree-emit.sh (_resolve_gui_state_path / _main_repo_root)
- **Status:** closed
- **Description:** First end-to-end run: the wired emitter (running in a worktree) wrote `branch-opened`/`concluded` to the *worktree's* module `tree-state.json` (resolved via `git rev-parse --show-toplevel`), but the operator runs ONE GUI server started from the *main checkout*, which `fs.watch`es the main-checkout module file — so the GUI never updated (36 nodes, unchanged). The hook logic was otherwise correct (both worktree sinks received the events; idempotency + conclude verified). Root cause: a worktree-rooted writer must target the file the operator's single main-checkout GUI server watches. **Fixed in the same change:** added `_main_repo_root` (parent of `git rev-parse --git-common-dir`, the same pattern git-discipline.md uses for post-merge sync) and `_resolve_gui_state_path` → main-checkout module file; the §5 gate sink stays worktree-local (gate parity preserved). Re-verified live: GUI 36→38 nodes, `open`→`concluded` observed via the running server. Regression locked by self-test ST13 (GUI sink from a real `git worktree` → main checkout) + ST14 (gate sink stays §5/worktree, differs from GUI sink). Class: writer-path-vs-watcher-path-divergence-under-worktree-topology. Sweep query: `rg -n 'rev-parse --show-toplevel|git-common-dir|STATE_FILE|_resolve_.*state' ~/.claude/hooks/conversation-tree-*.sh` — note: the conv-tree GATES intentionally read the worktree §5 path (gate parity); only the GUI sink needed the main-checkout resolution. **Process note (surfaced, not acted on unilaterally):** the `--self-test` masked this because ST1–ST12 use the `CONV_TREE_STATE_PATH` override, which short-circuits sink resolution — only the real end-to-end against the running GUI exposed it. ST13/ST14 now exercise the un-overridden path; the broader discipline (self-tests that override the production resolver cannot certify the resolver) is a friction worth a harness-testing-discipline discussion.

### NL-FINDING-010 — conversation-tree-state-gate blocks bare Task/Agent sub-agent dispatches that carry no Pin-1 candidate token (pre-existing gate-design gap)
- **Severity:** warn
- **Scope:** canon
- **Source:** functionality-verifier (live: the gate blocked a task-verifier `Agent` dispatch during dispatch-conv-tree-event-emission verification)
- **Location:** adapters/claude-code/hooks/conversation-tree-state-gate.sh (Pin-1 candidate extraction, ~lines 300-330)
- **Status:** dispositioned-defer
- **Description:** `conversation-tree-state-gate.sh` (shipped conversation-tree-ui v1, ac585c4) fires PreToolUse on `mcp__ccd_session__spawn_task|mcp__ccd_session_mgmt__start_code_task|Task|Agent` and ALLOWs only if the attested `snapshot.nodes[]` has a live node whose `node_id`/`title` equals a Pin-1 candidate extracted from `tool_input`: (1) `task-id=<tok>`, (2) `worker-<tok>`, (3) backtick-token after "branch", (4) `.tool_input.title`. A bare `Task`/`Agent` dispatch with only a `prompt`/`description` (e.g. a `task-verifier`, `explorer`, `code-reviewer` sub-agent — the orchestrator pattern's bread and butter) carries NONE of the four → the gate's candidate set is EMPTY → it BLOCKs unconditionally, and **no writer can satisfy it** (there is no candidate string to name a node after). Impact: any session where the gate is live cannot use the orchestrator pattern's sub-agent dispatch without the gate's `conv-tree-spawn-waiver-*.txt` valve per dispatch. **What the conv-tree-emit writer DID fix (this change):** candidate-BEARING spawns now auto-pass — the emitter (wired immediately before the gate) computes the gate's exact Pin-1 candidate set and titles the emitted branch with the primary candidate, so `mcp__ccd_session__spawn_task` (the real Dispatch shape, always has `.title`) and orchestrator `worker-`/`task-id=` dispatches genuinely satisfy the gate (proven live: `[conv-tree-gate] ALLOW: verified snapshot names live branch node`). The bare-prompt `Task`/`Agent` case is NOT writer-fixable. **Disposition: defer** — out of scope to change the gate here (gate behavior is explicitly out-of-scope for the emitter plan; gate-respect.md: don't unilaterally rewrite a gate mid-task). Candidate gate-side fixes for a future harness-improvement plan: (a) the gate also accepts a node whose `node_id`/`title` matches a stable hash of the bare prompt (the emitter already emits `sp-<sha1(sid|title|bucket)>` — the gate could derive the same), or (b) the gate treats `subagent_type`-only dispatches (read-only verifier/reviewer agents) as exempt, or (c) the orchestrator-pattern dispatch convention always injects a `task-id=` sentinel (composes with spawn-task-report-back.md). Class: gate-pin1-candidate-extraction-misses-bare-subagent-dispatch. Sweep query: `rg -n 'CANDS\+=|task-id=|worker-|tool_input.title' ~/.claude/hooks/conversation-tree-state-gate.sh`. **Note:** the gate's own documented remediation #2 (fresh substantive `conv-tree-spawn-waiver-*.txt`, ≥1 line, <1h) is the sanctioned interim path for legitimate bare-subagent dispatches per gate-respect.md — used during this session's task-verifier verification with substantive justification.

### NL-FINDING-011 — v1.2: Dispatch-side state-file reader to auto-surface GUI inline-response events in Dispatch chat
- **Severity:** info
- **Scope:** spec
- **Source:** Misha (live GUI use, 2026-05-18; explicitly scoped OUT of conv-tree-ui v1.1 item 11)
- **Location:** neural-lace/conversation-tree-ui/ (new Dispatch-side reader) + adapters/claude-code/hooks/ (likely a SessionStart/poll surfacer)
- **Status:** open
- **Description:** conv-tree-ui v1.1 item 10 ships GUI-side inline response capture: a "Respond" affordance on decision/question actions POSTs a new `action-responded` event (response text + target action_id), the action moves to "responded — awaiting confirmation", and a "Copy to Dispatch →" button formats the payload for manual paste into Dispatch chat. v1.1's compromise is **manual paste closes the loop**. v1.2 builds the missing automatic half: a Dispatch-side reader that watches the conversation-tree state file for new `action-responded` events and surfaces them in the active Dispatch session's chat automatically (so Misha's GUI response reaches the running Claude without a manual copy-paste round-trip). Explicitly OUT of v1.1 scope per Misha ("Don't try to build the Dispatch-reader here — too much scope"). Composes with `spawn-task-report-back.md` (the orchestrator-callback substrate) and the Decision-011 cloud/local enforcement split. Class: dispatch-side-state-reader-missing. Sweep query: `rg -n 'action-responded' neural-lace/conversation-tree-ui/`.

### NL-FINDING-012 — PR #11 (conv-tree-ui v1.1.1 items 14-23) merged to master with a failing backfill self-test (B15)
- **Severity:** warn
- **Scope:** unit
- **Source:** regression-run during conv-tree-ui-v1.1.2-polish merge reconciliation (2026-05-18)
- **Location:** neural-lace/conversation-tree-ui/state/backfill-details.js (B15 self-test) — shipped by PR #11 (branch claude/jolly-davinci-d99487, master 759923d)
- **Status:** open
- **Description:** `node state/backfill-details.js --self-test` on pristine origin/master reports **14 passed, 1 failed** — `B15 payloadFor enriches from a resolvable same-repo doc` FAILS. Proven NOT introduced by v1.1.2: `git diff origin/master -- neural-lace/conversation-tree-ui/state/backfill-details.js` is byte-empty (file identical to master), and v1.1.2 does not touch backfill — its only state changes are the additive `item-backlogged` event + optional `deferred` local-time fields + P16, entirely unrelated to the doc-enrichment path B15 exercises (proven: state/selftest.js 16/16 incl P16, all v1.1.2-relevant suites green on the reconciled tree). PR #11's plan/PR presumably reported backfill green against an environment where the same-repo source doc resolved; B15 depends on a resolvable in-repo doc path that is absent on this machine. Out of v1.1.2 scope (flag-don't-fix per autonomy — not this work's regression, unrelated subsystem, non-trivial enrichment logic). Class: pr-merged-with-failing-self-test (a green-bar claim that doesn't hold on pristine master — same shape as the NL-FINDING-007 "10/10 false signal"). Sweep query: `rg -n "B15|payloadFor|resolveDocPath|extractFromDoc" neural-lace/conversation-tree-ui/state/backfill-details.js`. Recommended (dedicated future session): re-run #11's backfill against pristine master, diagnose whether B15 is environment-dependent (needs a seeded fixture doc) or a real enrichment regression, then either fix the enrichment or make B15 hermetic (self-seeded fixture, no reliance on a possibly-absent repo doc).

### NL-FINDING-013 — "v1.1.2" + plan-slug `conv-tree-ui-v1.1.2-polish` collision: two parallel Dispatch sessions used the same version/slug for UNRELATED work
- **Severity:** error
- **Scope:** cross-repo
- **Source:** drive-to-merge of PR #12 (this session, Misha's items 25-28) — discovered master advanced via PR #13
- **Location:** docs/plans/conv-tree-ui-v1.1.2-polish.md (this branch) vs docs/plans/archive/conv-tree-ui-v1.1.2-polish.md (master, via PR #13)
- **Status:** open
- **Description:** This session implemented Misha's explicit v1.1.2 items 25-28 (filled buttons / no-scroll Details / Respond-only decisions / Defer popover) on branch `conv-tree-ui-v1.1.2-polish` → PR #12. In parallel, the `claude/jolly-davinci-d99487` Dispatch session (which earlier shipped the items-14-23 superset, #11) ALSO created a plan at the SAME path `docs/plans/conv-tree-ui-v1.1.2-polish.md` and SAME version label "v1.1.2" for COMPLETELY UNRELATED work — its plan title: "v1.1.2 — live-feedback follow-up (drop item 20, add item 25=top-level tree headers)"; its items: 20R (revert promote-rename), 25 (tree-root header styling via renderTreeNode depth), 26 (selftest sweep). That effort merged FIRST (PR #13, master `e921ca8`/`6a7f20d`) and auto-archived the slug as COMPLETED. Net: Misha's requested items 25-28 are NOT on master (verified: master app.js has 0 `openDeferPop`/`item-backlogged`/`datetime-local`, still has quiet `'mark answered'`); PR #12 is the only place they exist and is NOT redundant; but the `conv-tree-ui-v1.1.2-polish` slug + "v1.1.2" name are now consumed by unrelated merged work. Root cause: parallel Dispatch worktree sessions independently chose the same next-version label + plan slug with no cross-session reservation; the conv-tree-ui module is a hotspot for this (3 parallel v1.1.x efforts: #10 items14-18, #11 items14-23, #13 "v1.1.2 drop-20"). Class: parallel-session-version-and-plan-slug-collision. Sweep query: `git log --all --oneline | rg "conv-tree-ui-v1\.1\.[0-9]"`. Resolution requires a USER decision (version/slug is product identity per CLAUDE.md naming rule) — surfaced to Misha (PAUSING); PR #12 left open, branch pushed, all v1.1.2(items25-28) suites green pre-collision.

### NL-FINDING-015 — Update record to NL-FINDING-012: B15 resolved on master by PR #13 (retitled 2026-07-03 from a non-schema hyphenated-suffix ID; findings-ledger-schema-gate enforcement)
- **Severity:** info
- **Scope:** unit
- **Source:** PR #13 completion report (in-flight B15 fix)
- **Location:** neural-lace/conversation-tree-ui/state/backfill-details.js
- **Status:** dispositioned-accept
- **Description:** NL-FINDING-012 (PR #11 merged with failing backfill B15) is now RESOLVED on master: PR #13's in-flight fix "repointed B15 from the v1.1.1-closure-archived plan path to the permanent docs/DECISIONS.md; backfill restored 15/15." NL-FINDING-012's recommended dedicated-session fix is therefore moot — B15 was environment/path-fragility (a referenced doc moved on plan-archive), fixed by repointing to a permanent doc. No further action; NL-FINDING-012 → dispositioned-accept (resolved upstream by #13).

### NL-FINDING-014 — Agent-tool `isolation: "worktree"` silently failed for one of 12 dispatches; builder branched in the MAIN checkout
- **Severity:** error
- **Scope:** cross-repo
- **Source:** Wave-C orchestrator session (2026-07-02), caught at the post-cutover `git pull` on the main checkout
- **Location:** platform Agent tool (worktree provisioning) + adapters/claude-code/hooks/teammate-spawn-validator.sh (validates the FLAG, cannot verify the resulting cwd)
- **Status:** open
- **Description:** During the NL Overhaul Wave-C parallel build, 12 builder dispatches all passed `isolation: "worktree"`. Eleven received worktrees; ONE (the C.4-CL5 builder) ran in the ORCHESTRATOR'S MAIN-CHECKOUT-adjacent context: its completion notification carried no worktree block, and its `git checkout -b worker-C.4-CL5` executed in the main checkout, moving that checkout's HEAD off master. Detected only when the post-cutover `git pull` behaved oddly (HEAD at a worker commit; local master still tracking the wrong remote). No work was lost (the builder's commit was intact and cherry-picked normally), but the failure class is severe in the general case: an unattended writer operating on the shared main checkout is exactly the GAP-51/sync-daemon failure shape (see B.12). Mitigation available today: builders' FIRST-ACTION contract could add `git rev-parse --git-common-dir` vs `--git-dir` self-check ("am I in a linked worktree?") and return BLOCKED if not. Class: isolation-flag-accepted-but-not-provisioned. Sweep query: `rg -l "isolation" adapters/claude-code/hooks/teammate-spawn-validator.sh` + orchestrator dispatch prompts.

### NL-FINDING-016 — PreToolUse commit-gates block entire compound Bash commands, including the fix they contain
- **Severity:** warn
- **Scope:** canon
- **Source:** nl-overhaul orchestrator session 2026-07-03, observed live during the Wave-C master merge (findings-ledger-schema-gate)
- **Location:** all PreToolUse Bash gates matching git commit (findings-ledger-schema-gate.sh, scope-enforcement-gate.sh, pre-commit-gate.sh chain)
- **Status:** dispositioned-act
- **Description:** A compound command of the shape \"<fix> && git add && git commit\" is pre-screened by PreToolUse commit-gates against the CURRENTLY-staged state; when the gate blocks, the ENTIRE command is prevented from running — including the fix at its start. The agent sees a block, re-runs, and the fix never lands (observed 3 consecutive times before diagnosis). Corrective encoded: separate fix-calls from commit-calls (doctrine note), and D.0 audits gate block-messages to state explicitly that the whole command was not executed. Disposition: folded into the D.0 gate-map audit of the nl-overhaul program.

### NL-FINDING-017 — install.sh mv-based backup fails on locked files; live manifest.json went silently stale across a cutover
- **Severity:** warn
- **Scope:** canon
- **Source:** D.5 operator-side cutover completion (orchestrator session 2026-07-03)
- **Location:** adapters/claude-code/install.sh (backup mv of ~/.claude/hooks) + live ~/.claude/manifest.json freshness
- **Status:** dispositioned-act
- **Description:** During the D.5 live cutover, install.sh aborted mid-run (mv of ~/.claude/hooks to the backup dir: Permission denied — file held open), leaving the live manifest.json at its C.5-era state; the doctor then reported 20 claim-honesty REDs against retired-gate entries that no longer exist on master. Root causes: (a) mv-based backup is not lock-tolerant on Windows — should copy-then-verify instead; (b) no freshness check ties live manifest.json to the repo manifest (doctor compares against the LIVE copy). Disposition: fix install.sh backup strategy + add a doctor manifest-freshness check (live vs repo hash) — E-wave item; folded into the E.0 spec mandate.

### NL-FINDING-018 — Wave-D D.5 attic relocation moved retired Stop gates but not hooks/lib/, breaking `source` in 13 archived gates
- **Severity:** warn
- **Scope:** unit
- **Source:** decision-context-gate self-test root-cause session (2026-07-03), spawned from the Wave-D D.4 relocation-note task
- **Location:** adapters/claude-code/attic/*.sh — 13 gates `source "${BASH_SOURCE[0]%/*}/lib/stop-hook-retry-guard.sh"` unguarded; adapters/claude-code/attic/lib/ was absent until this fix
- **Status:** dispositioned-act
- **Description:** The D.5 cutover (PR #73, master d6c0176) moved retired Stop gates to `attic/` but not the `hooks/lib/` directory they source relative to their own path. Thirteen attic gates source `lib/stop-hook-retry-guard.sh` with no `|| true` guard, so any execution from attic emits "No such file or directory" then `retry_guard_session_id: command not found`; two further libs (`nl-paths.sh`, `workstreams-state-resolver.sh`) are guard-sourced and degrade silently. Fix (this entry's commit): three one-line re-source shims at `attic/lib/` pointing at `../../hooks/lib/`, restoring runnability of all archived gates without modifying their bytes. Attribution evidence from `attic/decision-context-gate.sh --self-test` run in a linked worktree: 19 pass / 7 fail unfixed → 20 / 6 with shims → 29 / 29 with shims plus `DECISION_CONTEXT_SCHEMA`/`CONV_TREE_STATE_LIB` pointed at a checkout whose `neural-lace/workstreams-ui/node_modules` has zod installed. The separately-reported 6-scenario self-test failure (ST2/ST5×2/ST11/ST12/ST28) is therefore NOT a logic regression: it is the HARNESS-GAP-42 accumulated-state-vs-cold-environment class (zod unresolvable because linked worktrees do not share the main checkout's gitignored node_modules; the validator degrades to allow-plus-fallback by design), the same class for which this gate has sat in hooks-selftest.yml `KNOWN_FAILING_HOOKS` since 2026-06-02. Residual note: the allowlist entries for `decision-context-gate.sh` and `decision-context-replay.sh` are now vestigial — the live-path shim no longer contains the `--self-test` token and attic/ is not swept, so CI no longer discovers either; the entries are inert and can be dropped in a future workflow touch-up. (Originally drafted as NL-FINDING-017 in commit bb4e60a; renumbered to 018 on merge — master's 51af599 took 017 concurrently.)

### NL-FINDING-019 — work-integrity check (a) treats any plan-file touch as full-plan ownership; the in-flight scope-update mechanism makes this a two-gate trap
- **Severity:** warn
- **Scope:** canon
- **Source:** decision-context self-test root-cause session (2026-07-03), Stop-gate retry after PR #74 merged
- **Location:** adapters/claude-code/hooks/work-integrity-gate.sh check (a) (`_wig_check_evidence`; "session-touched" = any transcript Edit/Write under docs/plans/) interacting with scope-enforcement-gate.sh's in-flight-scope-update requirement
- **Status:** dispositioned-act
- **Description:** Two gates interact to trap a session that did everything right. The scope-enforcement gate blocks commits of files outside the active plan's declared scope; its documented remedy is appending a line to the plan's `## In-flight scope updates` section (nine precedent lines exist). But that one Edit marks the plan "session-touched" for work-integrity check (a), which at Stop then demands ALL of the plan's unchecked tasks be completed (20 here — the operator's Waves E/F) or `Status: ABANDONED` be set on the operator's live program plan — both wrong for a session whose only plan edit was the scope line the other gate required. Check (a) has NO waiver valve (only check (b) reads the `acceptance-waiver-<slug>-*.txt` per-session waiver), so the session cannot legitimately clear the block; the session-honesty gate compounds this by rejecting `DONE:` while any block fired, and retry-guard downgrade is refused while `DONE:` is claimed. Observed in the session that shipped PR #74 (master a9c1c69): scope line for `.pr-description.md` → check (a) hostage state at session end. Class: gate-interaction trap (same family as NL-FINDING-016's compound-command trap). Proposed fix (needs harness-reviewer before landing): exempt plan touches whose diff is confined to the `## In-flight scope updates` section from check (a) plan-ownership, OR extend the per-session waiver valve to check (a) with the same freshness/substantive-reason rules as check (b). Disposition 2026-07-03 (ADR 059, operator-approved): the waiver valve shipped on master via NL-FINDING-020 (214541e); the ADR 059 commit layers the D4 honesty scoping (COMPLETED-but-unchecked and checked-box-without-evidence remain unwaivable), ledger-logged waiver use for E.3/E.5 telemetry, and self-test scenarios 2b/2c/2d; the structural fix (session end-manifest replacing transcript-forensics plan-touch derivation) lands via program task E.12 (authored as E.9; renumbered at the Wave-E reconciliation merge).

### NL-FINDING-020 — work-integrity-gate check-a (unchecked tasks) has no waiver valve; multi-wave plans recreate the waiver-tax inside the touched plan
- **Severity:** warn
- **Scope:** canon
- **Source:** Wave-D orchestrator session 2026-07-03 — the gate's FIRST live block, observed minutes after cutover
- **Location:** adapters/claude-code/hooks/work-integrity-gate.sh (check a, _wig_block "check-a-pending"; _wig_check_waiver only covers check b's acceptance-waiver-<slug> pattern)
- **Status:** dispositioned-act
- **Description:** A session that legitimately touches a MULTI-WAVE plan (e.g. the nl-overhaul program, 19 unchecked tasks spanning Waves E/F) is blocked at Stop by check-a with only "complete them or set Status: ABANDONED" as remedies — both wrong for a plan designed to span many sessions. The D.2 spec's "orthogonal ACTIVE plan does NOT block" scenario fixed cross-plan waiver-tax but not within-touched-plan waiver-tax. The sanctioned exit today is the retry-guard's non-DONE downgrade path (honest PAUSING/BLOCKED markers ride out after threshold) — functional but noisy and indistinguishable from ride-through in the ledger. Remediation (E-wave): give check-a a per-session waiver valve (work-integrity-waiver-*) AND/OR marker-aware pass-through — a PAUSING/CONTINUING final marker with an exact ask should satisfy check-a for plans whose unchecked tasks belong to future waves. Sweep query: `grep -n "check-a-pending" adapters/claude-code/hooks/work-integrity-gate.sh`. UPDATE 2026-07-03: the waiver valve shipped in this same commit — check-a now honors a fresh (<1h, substantive) `work-integrity-waiver-<slug>-*.txt` in `.claude/state/` (worktree or common-dir parent), and the block message names the hatch + what did/did not execute, per ADR 058 D5 pin (d). Marker-aware pass-through remains the E.10 follow-up; a self-test scenario for the valve is also assigned to E.10.

### NL-FINDING-021 — external-monitor alerts render with empty anomaly fields ("/ routes anomalous ( healthy)"); 32 unacked duplicates accumulated
- **Severity:** warn
- **Scope:** unit
- **Source:** SessionStart surfacer output, observed 2026-07-03 (alerts dating to 2026-06-27, all principles-gate-r3 probe)
- **Location:** external monitor probe writer (principles-gate-r3 alert JSON) + ~/.claude/state/external-monitor-alerts/
- **Status:** open
- **Description:** Every alert in the backlog renders as "/ routes anomalous ( healthy)" — the anomaly-description field is EMPTY and the health field says healthy, i.e. the monitor is alerting on a non-anomaly with a malformed message. 32 identical unacked alerts accumulated (the audit's RC4 0%-consumption loop in miniature). The formatter/probe emitting an alert whose anomaly string is empty is the defect; E.1's digest dedup/auto-ack absorbs the noise but should not mask the formatter bug. Remediation: fix the probe's alert-emission condition (no alert when anomaly string is empty/healthy) and ack the 32 stale duplicates as one class; wire into E.1 dedup.

### NL-FINDING-022 — Workstreams Layer-C heartbeat is doc-claimed but has NO runtime trigger under any name (theater class, §10)
- **Severity:** warn
- **Scope:** canon
- **Source:** harness-reviewer pass on the D.5-remediation diff (2026-07-03) — verified via verbose `schtasks` query (zero tasks matching conversation|workstream|heartbeat|claude) + install.sh grep (registers none)
- **Location:** adapters/claude-code/doctrine/automation-modes-full.md:371 + workstreams-state-full.md:53 (claims, corrected in this commit); adapters/claude-code/hooks/workstreams-emit.sh (`--heartbeat` mode, orphaned); install.sh (missing registration)
- **Status:** dispositioned-act
- **Description:** Both doctrine files claimed `workstreams-emit.sh --heartbeat` (previously `conversation-tree-emit.sh --heartbeat`) runs every 5 minutes via a Windows scheduled task — the Layer-C backstop that concludes crashed-orchestrator branches. No such task is registered under EITHER name, and install.sh registers none: the heartbeat has no runtime trigger. This is the constitution §10 theater class (documented enforcement that does not fire) and part of why the operator experienced the Workstreams UI "never accurately keeping track" — stale branches never auto-conclude. Doc claims corrected in this commit (honest NOT-WIRED wording). Remediation decision for E-wave: WIRE (install.sh registers the scheduled task, doctor verifies it exists — preferred, the GUI is the one consumed channel) or DELETE the `--heartbeat` mode + Layer-C doc section. Sweep query: `rg -in 'scheduled task|Task Scheduler|every 5 min|--heartbeat' adapters/claude-code/doctrine/ adapters/claude-code/hooks/` then verify each claim against `schtasks` + install.sh.

### NL-FINDING-023 — gate messages naming remediation commands that do not perform the claimed action (pin-d "exact next command" defect class)
- **Severity:** warn
- **Scope:** canon
- **Source:** harness-reviewer pass on the D.5-remediation diff (2026-07-03), pr-template-inline-gate.sh:572-574
- **Location:** instance fixed this commit: adapters/claude-code/hooks/pr-template-inline-gate.sh warn body (told builders to run `validate-pr-template.sh --self-test` to validate their `.pr-description.md` — but --self-test runs the embedded case suite and never reads the user's file); class: any block/warn message body naming a command
- **Status:** dispositioned-act
- **Description:** ADR 058 D5 pin (d) requires the exact copy-pasteable next command that satisfies the gate; a command that runs but does something OTHER than claimed is worse than none — the builder gets a green self-test and wrongly concludes their file is valid, causing exactly the wasted push the gate exists to prevent. Instance fixed in this commit (message now sources the validator library and runs `validate_pr_body` against the file). Class sweep for E.10: `rg -n 'self-test' adapters/claude-code/hooks/*.sh` — inspect occurrences inside block/warn MESSAGE BODIES (vs actual self-test blocks) and verify each named command does what the message claims; extend to any other command-in-message idiom.

### NL-FINDING-024 — workstreams spawn writer→gate is a PreToolUse RACE: first spawn of a title can block spuriously (waiver-tax generator)
- **Severity:** warn
- **Scope:** canon
- **Source:** orchestrator session 2026-07-03 ~18:25Z — spawn_task "Launch NL overhaul Wave E (E.0–E.10)" BLOCKED by workstreams-state-gate ("verified snapshot has no live node naming this spawn's branch") despite the writer firing correctly in the same attempt
- **Location:** adapters/claude-code/hooks/workstreams-emit.sh (--on-spawn, PreToolUse entry 29) × workstreams-state-gate.sh (PreToolUse entry 30, same matcher); design comment in --on-spawn claims it "genuinely satisfies the gate that runs immediately after (the ADR-031 r7 intended design)"
- **Status:** open
- **Description:** PROVEN from ~/.claude/logs/conversation-tree-emit.log: at the blocked attempt the writer completed both sinks (18:24:57 main-checkout GUI sink, 18:24:58 the worktree tree-state.json — the exact file the gate's _resolve_state_path targets) and logged branch-opened sp-87e4cf216648 with the verbatim title; the gate still blocked. The retry ~6 min later passed with the SAME dedup'd node id — attempt 1's write persisted, so the pass needed no new state. HYPOTHESIZED mechanism (refuter: documentation/observation that Claude Code runs multiple matching PreToolUse hooks serially in listed order): matching PreToolUse hooks execute CONCURRENTLY, so the gate can read the state file before the writer finishes — the "writer runs immediately before the gate" design assumption does not hold, and every first spawn of a fresh title risks a spurious block. Incentive impact (ADR 058 pin e): the block message's path-of-least-resistance is the waiver (agents will not guess "just retry"); prior sessions' conv-tree-spawn-waiver files are consistent with this class firing before. Remediation candidates (E.10): gate performs its own write-then-verify inline (call the writer synchronously before checking — eliminates the race in one hook); or a bounded re-read retry (~2s) before blocking; or block message adds "if you JUST spawned this, the writer may still be flushing — retry once before waiving". Also note the live gate file still self-identifies as "conversation-tree-state-gate" in its block JSON (naming residue, same class as NL-FINDING-022's neighbors).

### NL-FINDING-025 — work-integrity-gate --self-test intermittently fails should-pass scenarios with exit 2 and EMPTY stderr (environmental flake; misattribution risk)
- **Severity:** warn
- **Scope:** unit
- **Source:** harness-reviewer pass on the ADR 059 check-a valve diff (2026-07-03) — 3 full-suite runs of the pre-reconciliation shape produced failures at s6+s8, then 2b, then none; 0 failures in 50 controlled A/B invocations of the same scenarios; reconciled HEAD suite green
- **Location:** adapters/claude-code/hooks/work-integrity-gate.sh --self-test harness (_run_gate/_expect; scenarios build synthetic git repos under a tempdir)
- **Status:** open
- **Description:** The full self-test suite intermittently fails scenarios that pass in isolation, with a signature no gate code path produces — exit 2 with completely empty stderr (every real block writes a stanza to stderr before exiting 2). HYPOTHESIZED environmental (tempdir/process churn under parallel load; refuted if 5 consecutive clean-machine full-suite runs stay green — the failing scenarios executed NONE of the changed code). Practical risk: a red CI or local run gets misattributed to whatever change is under review (it nearly was, during the ADR 059 valve review). Mitigation landed with this entry's commit: _expect now dumps last-stdout + last-stderr + the raw rc on failure so the next occurrence is diagnosable from its artifact. Remediation (E.2/E.10 candidate): isolate per-scenario tempdirs or serialize repo builds; investigate whether retry-guard state files under the shared tmproot leak across scenarios. Sweep query: run `bash adapters/claude-code/hooks/work-integrity-gate.sh --self-test` 5x consecutively; check hooks-selftest.yml CI history for intermittent reds on this hook.

### NL-FINDING-026 — existence-only waiver checks and remedy-artifacts-counted-as-dirt: two residual classes from the check-a valve review
- **Severity:** warn
- **Scope:** canon
- **Source:** harness-reviewer pass on the ADR 059 check-a valve diff (2026-07-03), Findings 1-generalization and 3
- **Location:** adapters/claude-code/hooks/work-integrity-gate.sh check (c) worktree-teardown waiver (existence-only check, ~line 560) + check (c) untracked-dirt computation (`git ls-files --others`) in repos whose .gitignore lacks `.claude/state/`
- **Status:** open
- **Description:** Class 1 (proven sibling): check (c)'s teardown waiver is honored on file EXISTENCE+freshness alone while its message implies substantive content — the same documented-constraint-not-enforced theater class the check (a) valve just fixed (check (a) and check (b) now share the parameterized `_wig_check_waiver` substantive-first-line rule; check (c) does not yet). Sweep: `rg -n "newermt" adapters/claude-code/hooks/*.sh` — audit each hit for a companion content check. Class 2 (hypothesized outside this repo, proven safe inside it via .gitignore:119): this is a global Stop hook, and in a governed repo that does NOT ignore `.claude/state/`, writing any waiver file (the sanctioned remedy) makes a linked worktree dirty → check (c) blocks → a 2-gate chain of the NL-FINDING-016/019 family. Fix candidates (E.10): route check (c) through the shared waiver helper; exclude `.claude/state/` from the untracked-dirt computation (one grep -v) or have the doctor verify the ignore rule in every governed repo. Generalization for the F.5 remedy-chain checklist: any gate whose remedy creates a state file must guarantee downstream gates do not count that file as a violation.

### NL-FINDING-027 — session-honesty gate reads RESOLVED work-integrity blocks as live: any block ever recorded this session forbids DONE forever
- **Severity:** warn
- **Scope:** canon
- **Source:** ADR 059 session (2026-07-03), final Stop — honesty gate refused `DONE:` although work-integrity PASSED that same Stop and the deliverable was merged (7971db2); independently observed + drafted by the Wave-D orchestrator session the same day (its uncommitted draft was salvaged at the Wave-E reconciliation; this entry is the firsthand original, kept on dedupe)
- **Status:** open
- **Location:** adapters/claude-code/hooks/session-honesty-gate.sh `done_contradicted_by_block()` — the "after the last DONE claim" ordering is self-admittedly "approximated as: the block record exists"
- **Description:** The DONE-after-block contradiction check queries the retry-guard unresolved log and the signal ledger for ANY work-integrity-family block record bearing this session's id, with no ordering or resolution check — so a session whose early blocks were all RESOLVED (files committed, PR merged, waiver honored; work-integrity exiting 0 at the final Stop) still cannot end with the truthful `DONE:` marker. The gate's designed remedy (demote the marker to PAUSING/BLOCKED) then makes the transcript's final line LESS accurate: the operator reads a BLOCKED marker on a session whose work is merged with SHAs cited. This is the ADR 059 D3 class (history-state assertion posing as a current-state one) plus the D6 forensics class (gate re-deriving session state from side-channel records instead of validating current claims). Golden scenario: this session — check (c) blocks at ~turn N (uncommitted work, fixed and committed each time), check (a) block (waived per NL-FINDING-020's valve), everything merged at 7971db2, work-integrity green at final Stop, honesty gate still contradicting DONE from hours-old ledger rows. Fix candidates: (1) minimal — only contradict when work-integrity ALSO blocked the current Stop (both gates run in the same chain; a same-stop marker file or ledger timestamp-vs-message-timestamp comparison suffices; the gate already extracts the final-message timestamp "used only to order the DONE claim against block records" but never uses it); (2) structural — E.11's batched verdict makes the chain share one current-state result, and E.12's end-manifest replaces side-channel forensics entirely (tasks renumbered from E.8/E.9 at the Wave-E reconciliation). Until fixed, sessions with any resolved mid-session block must end BLOCKED/PAUSING regardless of true state — a forced-inaccuracy tax the waiver-density/KPI telemetry will not see because the honesty gate's blocks are not waivable. Assignment (specs-e): the minimal fix + a DONE-riding regression scenario land in E.10; once fixed, register session-honesty-gate in RETRY_GUARD_VERIFICATION_HOOKS (today its blocks are threshold-downgradeable even while DONE is claimed — the ride-out path sessions will actually use while the FP class persists).
