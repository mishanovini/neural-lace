# Wave-O orchestrator drills (live estate, post-install @ master f25c33c, 2026-07-07)

## Q1-Q5 time-to-answer (sketch pre-registered metric: each <10s)
- nl status:   5243ms PASS (1 session, oracle od_sessions; Q4 header initially "doctor: cached never" -> cache refresh run)
- nl needs-me:  751ms PASS (oracle od_needs_me; 23 open -> 0 after artifact cleanup of end-manifest downgrade pollution)
- nl shipped:  1924ms PASS (64 commits since 24h anchor, oracle od_shipped_since)
- nl costs:   16364ms FAIL cold (>10s bar; O.3 livesmoke was 6.3s — estate-state dependent; verifier judging; remediation = tune OBS_COSTS_* defaults)
- nl backlog: 80073ms (no time bar in Done-when; open perf task task_89ee665d)

## O.2 kill-drill
Seeded C1 heartbeat (dead subshell pid, last_activity_ts now-45min) -> `nl status --json`
classified `drill-kill-o2 -> crashed` in 8980ms (within one refresh). PASS. File cleaned.

## Q6 (024-class forensics)
observability-derive.sh --self-test 42/42 incl. scenarios 8/8a/8b: `nl why` reconstructs the
seeded spawn-writer->gate chain (<=20 lines, names writer/gate/ordering/retry). Root retired
in O.4 composition (gates atticized); NL-FINDING-024 closed on master.

## Notes for O.7 retro
- Blocking-budget framing correction: canonical D-wave units 10/12 UNCHANGED by the retirement
  (spawn-gate was folded in the agent-teams unit); raw blocking:true+wired entries 15->14.
  Decision 060's "10->8" was the raw framing — canonical oracle wins (CANONICAL-COUNTERS-01).
- obs-writers-firing RED on back-to-back doctor runs with no intervening Stop = calibration
  question handed to the O.6 verifier.
- jq.exe CRLF output bit the orchestrator's own cleanup loop (O.3's documented class) — the
  finding generalizes beyond the lib.
