# Task 7 — CORRECTED-path honesty note (harness-review follow-up, 2026-07-13)

The live Task-7 run captured `CONVERGED` (the two remote masters were already
equal at run time). **No real FF-drift window occurred during the build**, so
the `CORRECTED` status path was NOT exercised against the real remote pair.

- The `CORRECTED` path IS proven — by the sandboxed self-test scenarios T2, T3,
  T10 (bootstrap→correct) and T11 (dual-pushurl topology→correct), each running
  against real local bare repos, asserting the mirror advanced and status
  `CORRECTED`.
- The one real-world interaction the fixtures cannot exercise — the corrective
  push traversing the **machine-global pre-push hook chain** (credential scanner
  + divergence check) — was identified by the harness review and addressed:
  `MASTER_DRIFT_PUSH_TIMEOUT` (default 60s, separate from the fetch bound) gives
  the chain headroom, and `_push_reject_reason` now classifies the divergence
  hook's block phrase (`advanced since your last fetch` / `DIVERGENCE CHECK`) as
  `non-ff` (self-healing), not generic `rejected`. See the PUSH-REJECTED triage
  section of `docs/runbooks/master-drift-autocorrect.md`.

First real CORRECTED capture will occur the next time a genuine one-sided master
push happens on this machine; the phase log + status file will record it.
