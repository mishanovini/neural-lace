# TAKEOVER BRIEF — 2026-07-31 (written against an approaching weekly-limit cutoff)

**Read this first on the new machine.** Everything below is on `origin`. Nothing
important lives only on the Mac mini any more.

## 0. The one command to get current

```bash
git fetch origin && git checkout wip/harness-hardening-2026-07-29 && git log --oneline -8
```

Branch `wip/harness-hardening-2026-07-29` @ `b864b37` is the integration branch.
`origin/master` is `17c0d4c` and is BEHIND it. The work is NOT on master.

## 1. Why nothing is on master, and why that is correct

`review-record-push-gate.sh` blocks pushes to `refs/heads/master|main` when the
push would land harness content with no review record. It does NOT gate other
refs (`STABLE_BRANCHES_REGEX='^refs/heads/(master|main)$'`, line 146). That is
why every branch below pushed with no override and no `--no-verify`.

To land on master you need review records, not a bypass. Two REFORMULATE verdicts
below are the current blockers.

## 2. Branches on origin holding unmerged work

| branch | head | what it is | state |
|---|---|---|---|
| `wip/harness-hardening-2026-07-29` | b864b37 | integration branch — everything landed | pushed |
| `worktree-agent-abad29bbc527b68a4` | f018623 | green-chip emitter fix | **REFORMULATE** |
| `worktree-wf_795d2726-dc4-5` | 2b6e6a5 | Stop-gate rewire | **REFORMULATE — DO NOT LAND, see §4** |
| `worktree-agent-a2605766de68f6102` | dfc9f75 | scope-gate parser fix | DONE, needs cherry-pick |
| `worktree-wf_795d2726-dc4-1` | ef3ea6b | /api/asks cure | 2 CRITICALs found IN it |
| `worktree-wf_795d2726-dc4-2` | 7ab78a4 | start-plan bash-4 empty-title fix | unreviewed |
| `worktree-agent-ade1646f3ab225e4b` | ccaee00 | review-gate chain | ALREADY cherry-picked |
| `worktree-wf_1d662efd-584-1` | db5fb49 | code-trace methodology | ALREADY cherry-picked |
| `worktree-wf_795d2726-dc4-6` | cbd5dd4 | decision 069 | ALREADY cherry-picked |

## 2b. LANE RESULTS — all pushed to origin, all reported honestly

Every lane wrapped and pushed. **Nothing below is merged to master.** Statuses are as
the builders reported them; where a builder said something is NOT closed, that is
carried through verbatim rather than rounded up.

| branch | SHA | result |
|---|---|---|
| `worktree-agent-aa1b945ce10be1de1` | `ab8055b` | emitter fix — **all 5 defects CLOSED** |
| `worktree-agent-a673acaf846b20802` | `3803255` | case-fold — **CRITICAL CLOSED**, Majors/Minors open |
| `worktree-agent-a46bb217cad90617d` | `a15f55a` | Stop-gate — **RCE CLOSED**, 2 Criticals + 3 Majors open |
| `worktree-agent-ac4e32a8699059b53` | `a7a7438`+`fae9285` | learning ledger — 68% → **0%** noise |
| `worktree-agent-a5b5ba5fff93e77b4` | `92264e9` | multi-IF checker — **5 of 7 items**, 2 untouched |
| `worktree-agent-a363d9600a490e595` | `5739301` | design-coverage reviewer — built AND trialled on 2 real plans |

**Resume points, per lane:**
- **Emitter (`ab8055b`)** — only remaining step is an in-suite regression pin for defect 4
  (scenario `RPL9`: `chmod 500` the ledger dir → assert WARN + `replay=?`). A working probe
  is at `scratchpad/prove-d4-red.sh`; no re-derivation needed. Suite 120p/1f (ST11
  pre-existing) on both interpreters.
- **Case-fold (`3803255`)** — scope was WIDER than the review found: 13 broken surface arms,
  not 5, including the `adapters/claude-code/` prefix strip itself. NFC/NFD measured on APFS
  and proven NOT exploitable (canonical decomposition never maps non-ASCII onto bare ASCII,
  and every surface string is pure ASCII); the real Unicode set is `{U+017F→s, U+212A→k}`,
  both closed. Lib suite 75/0 → 113/0. Open: both Majors, both Minors, and the manifest
  `bypass_paths[16](v)` rewrite. **New sibling found, HYPOTHESIZED not reproduced:**
  `hooks/local-edit-gate.sh:101-114` normalises separators but not case, and is
  `blocking: true`. Filed as `PATH-EQUIVALENCE-CLASS-01`.
- **Stop-gate (`a15f55a`)** — the attack is dead, proven by re-running it: the `curl -o`
  victim file and the `curl -T` exfil target both **EXIST at `2b6e6a5`** and are **ABSENT at
  `a15f55a`**. curl/sql are now UNVERIFIABLE unless an explicit `--allow-exec` flag is
  passed (deliberately a flag, not an env var, so a `settings.json` env block cannot silently
  re-arm it). **DO NOT MERGE**: `added_after` is still `"2026-04"`, so `harness-doctor.sh`
  skips this unit's whole §10 evidence bar and would go green on an unvalidated blocking gate.
- **Learning ledger (`a7a7438`)** — discriminator is `.origin.kind` on transcript entries
  (`human=72`, `task-notification=109`, `coordinator=34`, `peer=2`); nothing in the harness
  read it. **`harness-reviewer` did NOT run** (account spend limit) — this hook change is
  genuinely unreviewed and is supposed to be gated on it.
- **Multi-IF checker (`92264e9`)** — backward compatibility MEASURED across all 294 plan
  files: byte-identical exit code and output for every one. Honest caveat from the builder:
  only **3** plans carry a real `## Intended Functionality` section, so the content-sensitive
  surface was 3, not 294. Untouched: the template/doctrine update and the manifest
  `fp_expectation` re-measure. New E/F/G assertions are not yet mutation-proved.

- **Design-coverage reviewer (`5739301`)** — `agents/design-coverage-reviewer.md`, 442 lines,
  manifest entry `kind: pattern`, `blocking: false`, `honest_status` opening "PATTERN WITH NO
  INVOKER / CHOKEPOINT: NONE". It was actually RUN against two real plans, not just written:
  on a multi-functionality plan it derived 7 functionalities from the REDACTED text and found
  3 undeclared; on a plan that plausibly claimed zero functionality it **disproved the zero
  claim with cited consumers** — the "doc-only" edits change `docs/backlog.md`, which
  `session-start-digest.sh` and `discovery-surfacer.sh` read, so they change what the operator
  sees at every session start. That is the operator's own Phase-4 edge case working on real
  input. Untouched: the §6 chokepoint proposal and the doctrine cross-reference. Unreviewed
  (in-surface, and `review-independence.md` requires a different session anyway).

**A live defect this surfaced, worth fixing early on the new machine:** the scope gate
blocked a compound `cat > msg.txt <<EOF && git commit -F msg.txt`, so the heredoc never ran;
the retry then committed using a **stale `msg.txt` left in the shared session scratchpad by a
DIFFERENT session**, producing a commit whose message described unrelated work. It was caught
and amended, but `git commit -F <file>` silently succeeds on a stale file and the shared
scratchpad path makes collisions likely. This is NL-FINDING-016 reproducing live.

**A correction that matters for takeover:** `~/.claude/hooks/*.sh` and `~/.claude/scripts/*`
on the Mac mini are **stale COPIES, not symlinks** (verified with `test -L`) — contradicting
what §7 below says about the whole tree. Some of `~/.claude` is symlinked and some is not;
check per-path before assuming either way.

## 3. SECURITY — the findings that matter most

### 3b. The push gate on this branch HAS A LIVE HOLE (CRITICAL, fix in flight)

The gate landed in `782f2ba..802a937` can be bypassed by **one character of case**.
PROVEN end-to-end against a real bare remote with the gate as the actual pre-push hook:

```
git update-index --add --cacheinfo 100755,<blob>,adapters/claude-code/Hooks/injected.sh
```

(capital `H`) then commit and push → **rc=0, gate silent, lands on the remote** — and a
later clone materialises it at `adapters/claude-code/hooks/injected.sh`, inside the real
carrier directory alongside the genuine hooks, because the filesystem is case-insensitive.
Same for `Git-Hooks/pre-push`, `SCRIPTS/evil.sh`, `adapters/Claude-Code/hooks/evil.sh`,
`Manifest.json`.

`rrg_in_surface` (`hooks/lib/review-record-gate-lib.sh:138-211`) matches tree prefixes
case-sensitively; the surface it protects is realised case-insensitively. Only the
INJECTION direction is exploitable — clobber is not, because 0 of 311 in-surface canonical
paths contain an uppercase letter, so the real lowercase path always wins the checkout race.

**Do not treat this branch's gate as sound until that fix lands.** Unicode NFC/NFD on APFS
and Windows trailing dot/space are the same class and remain untested. Full row:
`SURFACE-PREDICATE-NARROWER-THAN-DELIVERY-IDENTITY-01` in `docs/backlog.md`.

### 3a. The Stop-gate rewire executes markdown

`2b6e6a5` (Stop-gate rewire) **must not land as written.** An adversarial review
PROVED that it executes `curl` command lines harvested from markdown files at
every session end, unattended:

- it ran `curl -o <path> file:///etc/hosts` from a fixture and the file was created;
- it **overwrote an executable hook script** the same way (→ next-session code execution);
- an *untracked* `.md` that merely QUOTES an evidence line is enough to trigger it;
- having someone else's branch checked out executes THEIR committed curl lines.

Only a metacharacter regex filters the line; `-o` (arbitrary write), `-T`
(upload/exfiltrate any readable file) and `-K` (arbitrary curl config) all pass.

**Current exposure: NONE.** The gate is not installed — live
`~/.claude/hooks/runtime-verification-gate.sh` is absent and the live dispatcher
still lists 3 member gates. It becomes live on the next `install.sh` /
`session-start-auto-install.sh` **if this branch reaches master**. Do not merge
`worktree-wf_795d2726-dc4-5` until curl/sql lines are classified UNVERIFIABLE
instead of executed — which is what the gate's own header already claims it does.

The operator ORDERED this rewire ("Rewire it! Make it work!"), so the fix is to
make it safe, not to retire it.

## 4. LIVE-STATE HAZARD — a hook is installed from an unmerged branch

`~/.claude/hooks/workstreams-emit.sh` on the Mac mini is byte-identical to
`f018623`'s blob (sha256 `031fe941…`); master's blob differs. It was hand-installed
2026-07-30 20:05 PDT. `session-start-auto-install.sh` is **master-wins** for
`hooks/*.sh` (its own self-test scenarios L4/L6 assert this), so the next
SessionStart will silently copy master's UNFIXED content back over it, leaving a
backup nobody reads.

Consequence: the green-chip fix is live on that one machine only, and is one
SessionStart away from reverting. Merging to master is what makes it durable.
**On a new machine the fix is simply not present.**

## 5. Two REFORMULATE verdicts — what has to change before a review record exists

### 5a. Green-chip emitter fix (`f018623`)
The mechanism is **PROVEN correct on every path**, mutation-tested (2 mutants,
6 assertions reddening each), and confirmed against production: in the clean
post-install window, 586 dispatches → 579 replay suppressions → **0 phantom
greens**. Against the 3 hours prior on the old hook: 1880 `task_started`, with one
operator-only task receiving 40 events.

Blocking defects (all in the artifacts, not the mechanism):
1. **Critical** — `docs/backlog.md:2400-2401` claims the WARN counter "survives log
   rotation." It is `grep -c` over the rotating file, so it resets. Retract or implement.
2. **Major** — the WARN message says "this session"; the count is machine-wide.
3. **Major** — "all 105 dispatch identities are distinct" is vacuous: the ledger is
   deduplicated by the gate under discussion, so it cannot contain a counterexample.
4. **Major** — the replay-gate's own state write is `|| true`-silenced. If the ledger
   is unwritable the gate silently fails open and still logs `replay=0`.
5. **Major** — the backlog headline still says "NOT INSTALLED"; see §4.

Blob shas for the record once fixed: hook `b4a707de…`, doctrine `6ef52113…`,
backlog `59c19444…` (all currently UNCOVERED).

### 5b. Stop-gate rewire (`2b6e6a5`)
Beyond §3: `fp_expectation` says "near-zero, by measurement" but the measurement
**excluded the curl/sql population** — re-measured, 16 failures / 9 passes = 64%
failure on real, TRUE evidence. `added_after: "2026-04"` on a brand-new enforcing
artifact makes `harness-doctor.sh:2354` skip the §10 evidence bar entirely. No
waiver parity with sibling verification-class gates, so a false positive under a
`DONE:` marker has no valve.

## 6. Awaiting the operator (`NEEDS-YOU.md`) — 4 open

- attribution-gate VETO (`NY-1785461708-830f`) — not merged, nothing blocks yet
- decision 069 doctrine/** review surface (`NY-1785468489-b4eb`)
- live-settings dead hook — needs `grant-local-edit`; **machine-local to the Mac mini, not portable**
- Windows coord ps1 (`NY-1785425479-0d4d`) — actionable now; the script IS on master

**ANSWERED, do not re-ask:** Stop-gate rewire-vs-retire. The operator said "Rewire
it! Make it work!" Resolved in the ledger.

## 7. Machine constraints that do NOT transfer

The Mac mini has `~/.claude/*` as SYMLINKS into `adapters/claude-code/`, so
`install.sh` must never run there. **A normal machine does not have this** — check
`ls -la ~/.claude/hooks | head` before assuming either way.

Portability floor is `/bin/bash` 3.2.57 (macOS). No `${v^}`, `${v,,}`, `declare -A`,
`mapfile`, `date -d`. Run suites on BOTH interpreters by ABSOLUTE PATH, sequentially,
and drive the script under test via `$BASH` — otherwise the 3.2 column tests only the
harness, never the script body. That defect was found twice today in shipped suites.

`zsh` is the interactive shell: `"$var:refs/..."` is parsed as `${var:r}` + `efs/...`.
Use `"${var}"`. This silently broke 10 branch pushes today.

## 8. Where the real state lives

- `SCRATCHPAD.md` — session working memory, rewritten at milestones
- `NEEDS-YOU.md` — canonical awaiting-operator ledger (mechanically maintained;
  edit via `bash adapters/claude-code/scripts/needs-you.sh`, never by hand)
- `docs/backlog.md` — every open CRITICAL
- `docs/designs/end-to-end-process.md` — the 10-stage process design. Stage 0 is BUILT
  and blocking (`if-statement-check.sh` + `plan-reviewer.sh` Check 19); stages 1-9 are not
- `docs/designs/functionality-coverage.md` — one IF statement per FUNCTIONALITY rather than
  per plan (operator directive 2026-07-31). §7 is an honest build scorecard; §6 states
  plainly that the reviewer half has no invoker and is a Pattern until it gets one
- `docs/designs/code-trace-methodology.md` — 12 of 14 defects statically traceable
- `docs/decisions/069-doctrine-review-record-surface.md`
